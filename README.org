#+title: NixOS Dotfiles
#+startup: overview

* What This Is
This repository is the declarative NixOS and Home Manager configuration for
Skydive's machines.  The MSI laptop is the primary, daily-used system; the AMD
desktop output is a guarded bootstrap target, not yet a second fully accepted
installation.

The goal is a system that is understandable, recoverable, and pleasant to use.
=main= is the checkpoint lane.  New work belongs in small branches with source,
build, activation, runtime, and hand-test results kept distinct.

* Current Direction
- =nixosConfigurations.nixos= owns the MSI/NVIDIA laptop.
- =nixosConfigurations.desktop-amd= is the AMD desktop bootstrap output.
- MSI hardware facts are tracked under =hosts/msi/=; MSI evaluation, builds,
  and the =nrs=/=nrb= rebuild aliases use pure flake syntax.
- Hyprland is configured in Lua.
- Quickshell owns the bar, launcher, clipboard, wallpaper, OSD, tray, media, and
  status surfaces.
- =scripts/theme-select= and =theme/styles.json= own global theme state;
  Quickshell is a consumer, not the palette authority.
- Clean Emacs is the active personal Emacs configuration.  Doom is retired.
- NVF remains the package, plugin, and LSP owner for Neovim; live Sky behavior
  stays under =config/nvim/=.
- Ollama is retired from the intended system configuration.
- Vial and QMK are accepted MSI capabilities.  The QMK udev integration is
  active, Vial discovers the Corne v4, and the QMK CLI intentionally remains
  system-wide.
- VS Code is not configured on NixOS.  A future Linux port should adapt the
  macOS Home Manager module rather than copy Darwin-specific pins verbatim.

The accepted baseline is recorded in
[[file:docs/handoffs/2026-07-18-main-checkpoint.org][the main checkpoint handoff]];
later accepted maintenance evidence is in the
[[file:docs/handoffs/2026-07-19-vial-qmk-udev.org][Vial/QMK handoff]] and
[[file:docs/handoffs/2026-07-19-msi-pure-builds.org][MSI pure-build handoff]].
The accepted and promoted Quickshell cleanup sequence through Slice 1I is
recorded in
[[file:docs/handoffs/2026-07-25-quickshell-slice1i-acceptance.org][the Slice 1I
acceptance handoff]].  It expires stale CAVA samples after a documented
500-ms deadline and clears live levels when the analyzer exits, allowing the
unchanged fallback animation to resume.  Runtime payload =f7107cb= passed the
separated source, fixture, pure-build, controlled source-live, lifecycle, and
visual gates on =eDP-1=; immutable acceptance evidence =f8a4c90= records them.
The preceding Slice 1H network-handoff correction remains recorded in
[[file:docs/handoffs/2026-07-25-quickshell-slice1h-acceptance.org][the Slice 1H
acceptance handoff]].  Its real Wi-Fi/Ethernet handoff remains deferred until
Ethernet provides a safe alternate route.
The preceding Slice 1G window-close correction remains recorded in
[[file:docs/handoffs/2026-07-25-quickshell-slice1g-acceptance.org][the Slice 1G
acceptance handoff]].
The preceding Slice 1F clipboard correction remains recorded in
[[file:docs/handoffs/2026-07-24-quickshell-slice1f-acceptance.org][the Slice 1F
acceptance handoff]].
The preceding Slice 1E timer correction remains recorded in
[[file:docs/handoffs/2026-07-23-quickshell-slice1e-acceptance.org][the Slice 1E
acceptance handoff]].
The preceding stable CAVA work remains recorded in
[[file:docs/handoffs/2026-07-22-quickshell-slice1d-acceptance.org][the Slice 1D
acceptance handoff]].  Fresh dual-output validation and removal of the old
temporary configs remain separate work.
The conservative GC policy, complete first-run preflight, and observed manual
run are recorded in the
[[file:docs/handoffs/2026-07-19-msi-nix-gc.org][MSI Nix GC handoff]].
The separately accepted Nix-store optimization preflight and clean one-shot run
are recorded in the
[[file:docs/handoffs/2026-07-19-msi-nix-store-optimization.org][MSI Nix store
optimization handoff]].  Recurring optimization is deferred pending a
read-only accumulation measurement no earlier than =2026-08-18= and after at
least two actual scheduled GC runs; no optimizer timer is armed.

* System Shape
| Surface | Owner | Main source |
|---------+-------+-------------|
| Hosts and hardware | NixOS | =hosts/= and =system/modules/gpu/= |
| System packages/services | NixOS | =system/modules/= |
| User packages/services | Home Manager | =home/modules/= |
| Hyprland | live Lua config | =config/hypr/hyprland.lua= |
| Desktop shell | Quickshell | =config/quickshell/= |
| Global theme | selector scripts and registry | =scripts/theme-select*=, =theme/= |
| Emacs | clean live config plus Nix tools | =config/emacs/=, =home/modules/programs/emacs.nix= |
| Neovim | NVF plus live Sky Lua | =system/modules/programs/nvim/=, =config/nvim/= |

Several high-churn configs are intentional out-of-store links into this
checkout.  Editing them can change the running applications without changing a
Nix generation.  Always validate the actual linked bytes as well as the Nix
build.

* Repository Map
#+begin_src text
flake.nix          flake inputs and host outputs
hosts/             machine-specific entrypoints and hardware facts
system/modules/    reusable NixOS policy
home/modules/      Home Manager policy and live links
config/            application-native configuration
scripts/           user commands and theme orchestration
theme/             shared style registry and Nix tokens
docs/              current contracts, handoffs, audits, and history
#+end_src

* Build And Activation
Run the build gate from the repository:

#+begin_src sh
nix --extra-experimental-features 'nix-command flakes' \
  build --print-out-paths \
  '.#nixosConfigurations."nixos".config.system.build.toplevel' \
  --no-link --no-write-lock-file --show-trace
#+end_src

That command does not activate anything.  Live activation is separate:

#+begin_src sh
nrs
#+end_src

After activation, record the new generation and test the affected runtime
surfaces before calling the live result accepted.  Preserve a known rollback
generation until those checks pass.

* Next Work
Work stays slice-based:

1. Apply confirmed NixOS maintenance, security, service, and GC corrections one
   small change at a time.
2. For Quickshell, cleanup through Slice 1I and Slice 0C's read-only profiling
   helper are accepted and published.  Use =scripts/quickshell-profile= to take
   fresh, action-labelled measurements before making new performance claims.
   Slice 2 service and theme lifecycle is the selected candidate; it preserves
   the accepted live links and changes only startup ownership, normal theme
   reload behavior, service description, and restart backoff.
   Run the deferred real network-handoff check only after Ethernet provides a
   verified alternate route.  Keep dual-output validation, old CAVA temp-file
   deletion, clipboard lifecycle, shared ownership, and rendering or frame-rate
   work separate.
3. Give Emacs measured, real-use-driven TLC; Lua and Nix LSP slices are accepted,
   while Python's manual acceptance remains in progress.
4. Port the macOS VS Code configuration only as a Linux-specific Home Manager
   slice.

* Documentation
- [[file:docs/README.org][Documentation dashboard]]
- [[file:docs/projects/nixos-dotfiles.org][Current NixOS system map]]
- [[file:docs/HISTORY.org][Project history and decision log]]
- [[file:docs/areas/git.org][Git and checkpoint workflow]]

Project and area documents describe durable ownership.  Handoffs record active
execution state.  Audits are dated evidence, not standing authorization to
change the machine.
