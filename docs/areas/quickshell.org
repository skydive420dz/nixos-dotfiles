* Quickshell
:PROPERTIES:
:CUSTOM_ID: quickshell
:END:
This is the desktop shell layer: bar, OSD, launcher, clipboard, tray, media, and small status surfaces.

The measured architecture and candidate optimization slices are recorded in
[[../audits/2026-07-14-quickshell-optimization.org][the 2026-07-14 Quickshell optimization audit]].
That audit is an evidence baseline, not permission to override the ownership,
branch, or live-test contracts in this file.  The active checkpoint sequence is
recorded in [[../handoffs/2026-07-18-main-checkpoint.org][the 2026-07-18 runway]].
The accepted Slices 0A-1B base gates are recorded in
[[../handoffs/2026-07-19-quickshell-cleanup-activation.org][the 2026-07-19
activation handoff]].  That handoff records their accepted source-live
checkpoint, published evidence, and completed =main= promotion.
The read-only post-promotion classification of unfinished deterministic work is
recorded in
[[../audits/2026-07-19-quickshell-slice1-remainder.org][the 2026-07-19 Slice 1 remainder audit]].  Its proposed micro-slices are
not implementation authority.
The current accepted Slice 1C delta and its separate source, build,
source-live, runtime, and hand-test evidence are recorded in
[[../handoffs/2026-07-20-quickshell-slice1c-acceptance.org][the 2026-07-20
Slice 1C acceptance handoff]].  That handoff also records the completed strict
fast-forward promotion through documentation head =e4438b7=.  None of these
records authorizes another ref update, live change, dual-output test, or later
slice.
The accepted Slice 1D tracked-CAVA delta is recorded in
[[../handoffs/2026-07-22-quickshell-slice1d-acceptance.org][the 2026-07-22
Slice 1D acceptance handoff]].  Payload =ae1736b= passed its separate source,
pure-build, source-live, automated-runtime, and hand-test gates on =eDP-1=;
acceptance evidence is published at =89a7255=.  Three controlled pause/play
cycles created no new matching CAVA temporary configs.  Fresh dual-output
validation remains deferred; deletion of the 387 historical matching files
remains a separate mutable-state gate.  Strict non-forced integration through
=work/quickshell-cleanup= and =main= completed on 2026-07-23.  The immutable
pre-payload rollback anchor remains =9377dba= even though the moving branch
refs advanced.

The preceding accepted and promoted Slice 1E overlay-timer correction is
recorded in
[[../handoffs/2026-07-23-quickshell-slice1e-acceptance.org][the 2026-07-23
Slice 1E acceptance handoff]].  Payload =84b111b= stops a pending close timer
when either the launcher or clipboard is reopened.  Its exact-source and pure
no-link build gates remained separate from controlled source activation and
automated live interaction on =eDP-1=.  Both overlays preserved a sentinel
through a close/reopen interval shorter than 150 ms and still cleared after an
ordinary close.  The MSI checkout is clean on =main= carrying that payload,
Quickshell is healthy, and generation 860 is unchanged.  At the
pre-documentation snapshot, local and GitHub =main= and
=work/quickshell-slice1e= resolved to =84b111b=, while
=work/quickshell-cleanup= and =work/quickshell-slice1d= remained at =51c0396=.

Slice 1F is accepted and promoted at runtime payload =0022620=; exact evidence
and claim boundaries are in
[[../handoffs/2026-07-24-quickshell-slice1f-acceptance.org][the 2026-07-24 Slice
1F acceptance handoff]].  The one-file payload hardens clipboard preview
publication and stale-request handling and makes post-delete selection
deterministic.  Exact-source checks, the isolated preview/delete fixture, a
separate pure build, controlled source activation, remote synthetic
hand-equivalent preview testing, restored-state checks, and user acceptance
passed.  The user waived a separate physical hand test.  The live UI Delete
path was not exercised; cleanup removed only synthetic test rows, while all
eight filtered and unfiltered cases passed in the isolated fixture.  Published
acceptance evidence =6ea87f5= was integrated through
=work/quickshell-cleanup= and =main= by strict non-forced fast-forward.  The
live checkout is clean on =main= with the identical accepted QML, Quickshell is
healthy, and generation 860 is unchanged.  The two Slice 1F worktrees were
removed while their published evidence refs were retained.

Slice 1G is accepted and promoted at runtime payload =e8358af=; exact evidence
and boundaries are in
[[../handoffs/2026-07-25-quickshell-slice1g-acceptance.org][the 2026-07-25 Slice
1G acceptance handoff]].  The one-file payload schedules an authoritative
active-window refresh after =closewindow= and suppresses stale query results.
The rollback defect, eight isolated event/query scenarios, separate pure
build, controlled source activation, live A/B close sequence, restored state,
and human visual test passed.  Published acceptance evidence =180d7c2= was
integrated through =work/quickshell-cleanup= and =main= by strict non-forced
fast-forward.  The live checkout is clean on =main= with identical accepted
QML, Quickshell is healthy, and generation 860 is unchanged.  The two Slice 1G
worktrees were removed while their published evidence refs were retained.

Slice 1H is accepted and promoted at runtime payload =ead19fe=; exact evidence
and boundaries are in
[[../handoffs/2026-07-25-quickshell-slice1h-acceptance.org][the 2026-07-25 Slice
1H acceptance handoff]].  The one-file payload resets all traffic baselines on
an actual device transition, tags each sample with its captured device and
revision, rejects stale results, and treats the first accepted sample as a
baseline.  The rollback defect, 14 deterministic handoff/reconnect scenarios,
separate pure build, controlled source activation, and live =wlp4s0= sampling
probe passed.  Quickshell is healthy and generation 860 is unchanged.  Real
disconnect/reconnect and Wi-Fi/Ethernet handoff remains deferred until Ethernet
provides a safe alternate management route.  Route selection,
native/shared ownership, dual-output validation, and performance work remain
separate.

Slice 1I is accepted and promoted at runtime payload =f7107cb= with immutable
acceptance evidence =f8a4c90=; exact evidence and boundaries are in
[[../handoffs/2026-07-25-quickshell-slice1i-acceptance.org][the 2026-07-25 Slice
1I acceptance handoff]].  The one-file payload timestamps accepted analyzer
rows, expires stale levels after 500 ms, clears state on inactivity or process
exit, and lets the existing fallback resume.  The rollback reproduced both the
stall and exit defects; the payload passed 13 fixture checks, exact-source
validation, the separate pure build, controlled source activation, automated
lifecycle checks, and the human visual sequence on =eDP-1=.  The live checkout
is clean on =main= with identical accepted QML, Quickshell is healthy, and
generation 860 is unchanged.  Both Slice 1I worktrees were removed while the
published payload and evidence refs were retained.  Slice 0C's read-only
profiling helper is accepted and published at =41095ab=.  Its source checker,
pure no-link build, and stable no-action observation passed without changing
generation 860 or restarting Quickshell.  The paused, single-output result is
measurement evidence, not a performance claim.  Fresh dual-output validation,
automatic restart policy, historical CAVA cleanup, shared ownership, rendering,
frame-rate, and broader performance work remain separate.

Original Slice 3 is accepted end-to-end through shared workspace, active-title,
status, CAVA, and minute-aligned clock ownership.  Exact evidence is in the
[[../handoffs/2026-07-25-quickshell-slice3a-acceptance.org][Slice 3A]],
[[../handoffs/2026-07-25-quickshell-slice3b-acceptance.org][Slice 3B]],
[[../handoffs/2026-07-26-quickshell-slice3c-acceptance.org][Slice 3C]],
[[../handoffs/2026-07-26-quickshell-slice3d-acceptance.org][Slice 3D]], and
[[../handoffs/2026-07-26-quickshell-slice3e-acceptance.org][Slice 3E]] handoffs.
Root controllers now supply those five backend domains to both output bars while
per-output views and rendering remain local.

Slice 4A is accepted and promoted.  The root status controller now reads
default-sink volume and mute state through native PipeWire bindings instead of
a two-second Bash/=wpctl=/=awk= poller.  Views, controls, OSD scripts, and the
four other pollers were unchanged.  Exact evidence and claim boundaries are in
the
[[../handoffs/2026-07-26-quickshell-slice4a-acceptance.org][Slice 4A handoff]].

Slice 4B is accepted and promoted.  The same controller now reads display
battery presence, percentage, charge state, and global on-battery state through
native UPower bindings instead of the ten-second Bash/sysfs poller.  Existing
views and transition OSD behavior are unchanged; traffic, network, and
Bluetooth remain the three process pollers.  Exact evidence and claim
boundaries are in the
[[../handoffs/2026-07-26-quickshell-slice4b-acceptance.org][Slice 4B handoff]].

Slice 4C is accepted, published, and promoted.  Native Bluetooth adapter and
connected-device state replaced its 30-second shell poller without changing
the status view or click action.  Exact evidence and claim boundaries are in
the
[[../handoffs/2026-07-26-quickshell-slice4c-acceptance.org][Slice 4C handoff]].

Slice 4D replaces the 15-second network identity poller with native
connected-device/type and Wi-Fi signal state.  The unchanged two-second
traffic sampler is now the controller's only =Process=.  Selection means the
first connected supported device, not an exact default route; physical handoff
and traffic replacement remain separate.  Exact evidence and claim boundaries
are in the
[[../handoffs/2026-07-26-quickshell-slice4d-acceptance.org][Slice 4D handoff]].

Slice 4E1 native workspace occupancy is accepted, published, and promoted at
payload =f0ef9a1= with acceptance head =177a7fa=.  The shared workspace
controller now derives occupancy from native workspace toplevel lists;
per-output active workspace state, fixed buttons, styling, and Lua focus
dispatch remain unchanged.  Fresh dual-output validation remains deferred;
exact evidence and boundaries are in the
[[../handoffs/2026-07-26-quickshell-slice4e1-acceptance.org][Slice 4E1
handoff]].

Slice 4E2 native active-title state is accepted from source through live
runtime on =eDP-1=.  The controller now reads the active foreign toplevel's
=appId= and =title= directly, deleting the query, parser, timer, and raw-event
backend while preserving root ownership, normalization, layout, and styling.
The live-accepted payload =bbd7445= and current-main-aligned payload =a467e8b=
have identical patch IDs and controller bytes.  Acceptance head =27f525b= is
published and promoted by strict non-forced fast-forward, and the managed live
link resolves to promoted =main=.  Fresh dual-output validation remains
deferred.  Exact evidence and boundaries are in the
[[../handoffs/2026-07-29-quickshell-slice4e2-acceptance.org][Slice 4E2
handoff]].

On 2026-08-02, the startup wallpaper correction at =6de53d3= was accepted on
the current MSI activation.  =WallpaperStore= now starts with an empty media
path and lets persisted state choose the first wallpaper; the source checker,
separate pure no-link build, controlled runtime probe, and user visual check
all passed, with no wrong GIF observed.  This changes no package, dependency,
service, or wallpaper-selection ownership.

On 2026-08-03, native-plumbing cleanup code head =3f0f0c0= was accepted,
published, and promoted through documentation head =45589bc=.  It removes 461
net lines by replacing custom launcher discovery and signal-file toggles with
Quickshell desktop-entry and IPC APIs, using =FileView= for simple text reads,
and deleting dead weather, network-formatting, and inert QML code.  The
compatible OSD transport remains.  Source, separate no-link build,
rollback-protected runtime, strict promotion, generation-885 activation, and
post-activation launcher/clipboard gates passed on =eDP-1=.  Exact evidence and
boundaries are in the
[[../handoffs/2026-08-03-quickshell-native-cleanup-acceptance.org][native cleanup acceptance handoff]].

=main= remains the sober checkpoint shell.  The old modular shell lives on
=prototype/quickshell-legacy= and the archived Quickshell branches.  Treat
those branches as reference material only: port useful ideas back deliberately,
one module at a time.

** Branch Workflow
:PROPERTIES:
:CUSTOM_ID: branch-workflow
:END:
- =main= is the stable checkpoint for the known-good shell.
- Keep each candidate slice isolated through source, build, and live acceptance.
- Promote an accepted slice to =main= only through a separate strict
  fast-forward gate.
- Record acceptance documentation and retain or remove its evidence ref as
  separate decisions.
- Start the next small slice from promoted =main=.  Older cleanup and slice refs
  are evidence, not active integration lanes.

** Current Files
:PROPERTIES:
:CUSTOM_ID: current-files
:END:
- [[../../config/quickshell/shell.qml][Shell root]]
- [[../../config/quickshell/root/Bar.qml][Bar root surface]]
- [[../../config/quickshell/common/BarFrame.qml][Bar frame primitive]]
- [[../../config/quickshell/root/Clipboard.qml][Clipboard root surface]]
- [[../../config/quickshell/root/Launcher.qml][Launcher root surface]]
- [[../../config/quickshell/root/Osd.qml][OSD root surface]]
- [[../../config/quickshell/root/Wallpaper.qml][Wallpaper root surface]]
- [[../../config/quickshell/root/WallpaperPicker.qml][Wallpaper picker surface]]
- [[../../config/quickshell/common/Theme.qml][Theme tokens]]
- [[../../config/quickshell/qmldir][QML module index]]
- [[../../config/quickshell/modules/launcher/LauncherButton.qml][Launcher button]]
- [[../../config/quickshell/modules/launcher/LauncherView.qml][Launcher view]]
- [[../../home/modules/services/quickshell.nix][Quickshell service and custom desktop entries]]
- [[../../config/quickshell/modules/media/Media.qml][Media module]]
- [[../../config/quickshell/modules/media/MediaAnalyzer.qml][Media analyzer]]
- [[../../config/quickshell/modules/media/cava.conf][CAVA analyzer configuration]]
- [[../../config/quickshell/modules/media/MediaController.qml][Media controller]]
- [[../../config/quickshell/modules/osd/OsdView.qml][OSD view]]
- [[../../config/quickshell/modules/status/Battery.qml][Status battery]]
- [[../../config/quickshell/modules/status/Bluetooth.qml][Status bluetooth]]
- [[../../config/quickshell/modules/status/Clock.qml][Status clock]]
- [[../../config/quickshell/modules/status/StatusCluster.qml][Status cluster]]
- [[../../config/quickshell/modules/status/StatusController.qml][Status controller]]
- [[../../config/quickshell/modules/status/Network.qml][Status network]]
- [[../../config/quickshell/modules/status/NetworkGraph.qml][Status network graph]]
- [[../../config/quickshell/modules/status/StatusMetrics.qml][Status metrics]]
- [[../../config/quickshell/modules/status/Volume.qml][Status volume]]
- [[../../config/quickshell/modules/theme/ThemeToggle.qml][Theme toggle]]
- [[../../config/quickshell/modules/tray/Tray.qml][Tray module]]
- [[../../config/quickshell/modules/wallpaper/WallpaperButton.qml][Wallpaper button]]
- [[../../config/quickshell/modules/wallpaper/WallpaperSelector.qml][Wallpaper selector]]
- [[../../config/quickshell/modules/wallpaper/WallpaperStore.qml][Wallpaper store]]
- [[../../config/quickshell/modules/wallpaper/wallpaper-io][Wallpaper state and thumbnail publication helper]]
- [[../../config/quickshell/modules/workspaces/Workspaces.qml][Workspaces module]]
- [[../../config/quickshell/modules/workspaces/WorkspacesController.qml][Workspaces controller]]
- [[../../config/quickshell/modules/window/WindowTitle.qml][Window title module]]
- [[../../config/quickshell/modules/window/WindowTitleController.qml][Window title controller]]

** Commands
:PROPERTIES:
:CUSTOM_ID: commands
:END:
- =systemctl --user restart quickshell=
- =journalctl --user -u quickshell -f=

** Direction
:PROPERTIES:
:CUSTOM_ID: direction
:END:
Keep the shell small, calm, and portable. The bar is just a bar plus a few small apps; it should not become a 300-file desktop framework unless the complexity clearly earns its keep.

Current tree shape:

#+begin_src text
config/quickshell/
  shell.qml
  qmldir
  root/
    Bar.qml
    Launcher.qml
    Clipboard.qml
    Osd.qml
    Wallpaper.qml
    WallpaperPicker.qml
  common/
    BarFrame.qml
    Theme.qml
  modules/
    workspaces/
      Workspaces.qml
      WorkspacesController.qml
    window/
      WindowTitle.qml
      WindowTitleController.qml
    media/
      Media.qml
      MediaAnalyzer.qml
      cava.conf
      MediaController.qml
    status/
      StatusCluster.qml
      StatusController.qml
      Battery.qml
      Bluetooth.qml
      Clock.qml
      Network.qml
      NetworkGraph.qml
      StatusMetrics.qml
      Volume.qml
    tray/
      Tray.qml
    osd/
      OsdView.qml
    launcher/
      LauncherButton.qml
      LauncherView.qml
    clipboard/
      ClipboardView.qml
    theme/
      ThemeToggle.qml
    wallpaper/
      WallpaperButton.qml
      WallpaperSelector.qml
      WallpaperStore.qml
      wallpaper-io
#+end_src

This is descriptive, not a mandate to create empty folders. Split only when a module is ready to move.

** Ownership Rules
:PROPERTIES:
:CUSTOM_ID: ownership-rules
:END:
- =root/= owns top-level windows and surfaces only. =shell.qml= should wire root windows together, not host module behavior.
- =qmldir= is the portable QML project metadata. It exposes shared singletons and lets generic QML tooling identify =config/quickshell= as the project root.
- =.qmlls.ini= files are generated/runtime editor metadata and must stay ignored. Quickshell can populate them with machine-local build directories, so they are useful locally but are not repo truth.
- Keep this as a Quickshell config tree, not a packaged Qt library. Do not introduce a named QML module, CMake, or generated =.qmltypes= only to improve editor navigation. Use that machinery only if runtime packaging or reusable QML/C++ components need it.
- =common/= is for shared tokens and primitives only. It must not become a junk drawer for one-off component code.
- =common/Theme.qml= owns Quickshell design tokens: fonts, bar rhythm, shared radii, shared padding, shared icon/text sizes, and the current colors loaded from the global theme state.
- Quickshell is not the global theme authority. It may expose a selector UI, but the selected style belongs to =theme/styles.json= and =scripts/theme-select=. The selector must update the shared runtime bundle under =~/.config/theme/current/= so terminals, notifications, shell tools, and Quickshell converge on the same style.
- =modules/<name>/= owns the implementation details for that block: private helpers, local state, row components, timers, process calls, and popover behavior.
- Each block owns its own width, hover/open/selected state, click behavior, internal animation, and private click-out handling.
- The bar owns placement, global height, spacing between modules, and the visible bar input region.
- =modules/status/StatusMetrics.qml= owns the right-side status cluster sizing and graph geometry. Tune those metrics there first; do not spread one-off spacing constants across each status module.
- Global state is allowed only when multiple modules genuinely need it.

** Guardrails
:PROPERTIES:
:CUSTOM_ID: guardrails
:END:
- No module-specific state should live in =Bar.qml= once that module is split.
- No module should secretly resize, mask, or animate the whole bar.
- No full-screen invisible masks unless the surface is an explicit overlay or picker, and it must release clicks cleanly when closed.
- No phantom click areas: every layer should be as small as its visible, interactive surface unless there is a very clear reason.
- No module should reach into another module's private helper.
- Use shared theme tokens before local =Qt.rgba(...)= recipes.
- Keep fades, but be skeptical of height/width animation unless it explains a real spatial change.
- QML LSP support is best-effort editor tooling. Project structure should stay portable and runtime-first even if =qmlls= cannot jump every singleton property definition.
- Hyprland is Lua-first now. Do not use old hyprlang dispatcher strings such as =workspace 3= or =exec ghostty= from Quickshell. Use Lua dispatcher expressions such as =hl.dsp.focus({ workspace = "3" })= through =Hyprland.dispatch(...)= or =hyprctl dispatch 'hl.dsp....'=.

** Refactor Plan
:PROPERTIES:
:CUSTOM_ID: refactor-plan
:END:
1. Start from the working shell on =main=.
2. Create a cleanup branch before moving files.
3. Split one module at a time.
4. After each slice, rebuild/restart and test clicks, hover, focus, and monitor behavior before continuing.
5. Commit each stable slice separately.

** Next Audit
:PROPERTIES:
:CUSTOM_ID: next-audit
:END:
- Scan =config/quickshell= for modules reaching across ownership boundaries.
- Check whether sizing and spacing constants escaped =common/Theme.qml= or =modules/status/StatusMetrics.qml=.
- Confirm =root/Bar.qml= owns only surface placement, global bar layout, masks, and module placement, not module behavior.
- Check whether shared primitives in =common/= are genuinely reusable and not one-off module helpers.
- Document cleanup candidates before editing, then take one small slice at a time.

** Refactor Progress
:PROPERTIES:
:CUSTOM_ID: refactor-progress
:END:
- 2026-05-17: =modules/workspaces/Workspaces.qml= owns workspace state, =hyprctl workspaces -j= polling, Hyprland workspace events, rendering, and workspace click dispatch. =Bar.qml= only places =Workspaces {}=. The module keeps explicit implicit/Layout bounds. Workspace switching uses =Hyprland.dispatch("hl.dsp.focus({ workspace = ... })")=, because Hyprland 0.55/Lua no longer accepts the old =hyprctl dispatch workspace N= shape. Delegate files that read outer IDs should use =pragma ComponentBehavior: Bound=.
- 2026-05-17: =modules/window/WindowTitle.qml= owns active-window polling, Hyprland active-window events, title normalization, and title rendering. =Bar.qml= only places =WindowTitle {}=. This module has no pointer handlers or masks; its bounds are the visible pill only.
- 2026-05-17: =modules/media/Media.qml= owns the visible media pill and player controls. =modules/media/MediaController.qml= lives once at =ShellRoot= and owns player selection plus the =media.playPause= IPC target, avoiding duplicate =IpcHandler= registration from per-monitor bars. Inactive media has zero layout width, and prev/next only accept clicks when the player supports them. The prev/play/next controls use fixed-width icon wells so disabled controls stay visually present instead of collapsing or blending into the border. The root controller id must not share the same name as =Bar.mediaController=, or the delegate binding can shadow itself and leave media hidden.
- 2026-05-17: =modules/status/StatusCluster.qml= owns the current status behavior unchanged: clock, volume, network traffic, bluetooth, battery, battery plug/unplug OSD triggers, status polling, and click launchers for =nmtui=, =bluetui=, and =wiremix=. =Bar.qml= only places =StatusCluster {}=. Polling is intentionally unchanged in the extraction slice; optimize it only after the module passes live testing.
- 2026-05-17: Status polling was split after the extraction passed live testing. Keep the fast loop limited to cheap, visible-changing state: volume and traffic every 2s. Slower probes now run separately: network identity every 15s, battery every 10s, and bluetooth every 30s. This keeps service-heavy checks like =nmcli= and =bluetoothctl= out of the hot path without changing bar geometry or click targets.
- 2026-05-17: Network traffic labels use fixed four-character rates so the status pill does not visually twitch as traffic changes. Examples: =0000=, =012K=, =1.2M=, =042M=, =99M+=.
- 2026-05-17: The status pill grew from 324px to 340px and the network field minimum grew from 64px to 80px to fit the fixed-width traffic labels without clipping the clock/status fields.
- 2026-05-17: =Osd.qml= now owns only the top-level overlay layer, namespace, focus policy, and mask. =modules/osd/OsdView.qml= owns the signal watcher, JSON load process, hide timer, payload state, and card rendering. The root OSD mask still points to the visible card through =osdView.maskItem=, so this split should not expand the clickable/input region.
- 2026-05-17: Top-level surfaces moved into =root/=: =root/Bar.qml= and =root/Osd.qml=. =shell.qml= imports =root/=, =qmldir= now only exports the =Theme= singleton, and the root surfaces still own the only bar/OSD =PanelWindow= and mask definitions.
- 2026-05-17: =modules/tray/Tray.qml= owns the system tray item model, icon sizing, visibility, activation clicks, and native StatusNotifier menu clicks. =Bar.qml= only places =Tray {}= and passes its panel window so tray menus can anchor to the clicked icon. The tray still has no background, mask, hover panel, or global state.
- 2026-05-17: =modules/launcher/LauncherButton.qml= owns the left launcher button rendering, hover color, and click dispatch. Left click opens =launcher-toggle=; right click opens =clipboard-toggle=. =Bar.qml= only places =LauncherButton {}=.
- 2026-05-17: =modules/status/Clock.qml= owns status clock formatting, timer refresh, and clock text rendering. =StatusCluster.qml= only places =Clock {}= in the existing right-side status row.
- 2026-05-17: =modules/status/Bluetooth.qml= owns the bluetooth status icon, active color, fixed icon width, and =bluetui= click launcher. =StatusCluster= still owns the bluetooth polling process and passes only =available= and =connected= state into the view.
- 2026-05-17: =modules/status/Volume.qml= owns the volume status icon, muted color, fixed icon width, and =wiremix= click launcher. =StatusCluster= still owns the volume polling process and passes only =level= and =muted= state into the view.
- 2026-05-17: =modules/status/Battery.qml= owns the battery status icon, percent label, low-battery color, fixed battery width, and visibility. The plug/unplug OSD behavior stays in =StatusCluster=, which still owns battery polling and passes =level=, =charging=, and =status= into the view.
- 2026-05-17: =modules/status/Network.qml= owns the network status icon, traffic label rendering, disconnected color, layout width, text elision, and =nmtui= click launcher. =StatusCluster= still owns network identity polling and rate calculation, and passes =kind=, =downRate=, and =upRate= into the view.
- 2026-05-27: Bar visual checkpoint: the bar grew to a 34px rhythm with taller pills, the clock now uses a compact two-line time/date treatment, the media pill gained richer controls plus a dotted placeholder analyzer, and network traffic moved from numeric rates to a compact dotted graph. The media analyzer is still a fake animated placeholder; wire it to real audio levels later.
- 2026-05-27: Media analyzer is now wired to live CAVA output when media is active. =modules/media/MediaAnalyzer.qml= starts =cava= through =Quickshell.Io.Process= with a temporary raw ASCII config, parses the semicolon-delimited levels, and keeps the previous fake animation as a fallback when =cava= is missing or no live samples have arrived yet.
- 2026-05-27: The network pill click launcher now prefers =wlctl= and falls back to =nmtui=. =wlctl= is packaged locally as =pkgs/wlctl-bin.nix= from the upstream =v0.1.6= musl release binary because the upstream Nix/source build failed while fetching Rust crates from crates.io. Keep the local binary package until the source package is reliable enough to replace it.
- 2026-05-27: =wlctl= launches as =ghostty --class=wlctl -e wlctl=; Hyprland should match that class with the same centered Wi-Fi applet float rule as =nmtui=.
- 2026-05-27: The full bar/window-frame visual is drawn as one bar-owned surface to avoid translucent layer mismatches. =root/Bar.qml= owns the layer-shell surface, mask, placement, and module row; =common/BarFrame.qml= owns only the canvas path for the square outer bar/frame with Hyprland-radius inner cutout. Keep the bar mask pointed at =barRow= so the full-height decorative frame does not become a phantom click area.
- 2026-05-29: Status geometry was centralized in =modules/status/StatusMetrics.qml=. The current known-good right cluster uses a 240px total width, 6px module spacing, a 61px network slot, a 38px network graph, and a 9px network icon-to-graph gap. If the right pill drifts, adjust these shared metrics before touching individual module anchors.
- 2026-05-29: =modules/status/NetworkGraph.qml= owns the compact btop-inspired network graph. It draws dotted columns around a dotted middle baseline: download grows upward in =Theme.accent=, upload grows downward in =Theme.danger=, and each side scales from recent samples so low upload traffic stays visible instead of flattening into the baseline.
- 2026-05-29: =modules/status/Network.qml= owns dynamic network icon selection. Ethernet uses the wired icon, Wi-Fi uses signal-strength icons, and the disconnected state uses the no-network icon. Keep icon wells fixed-width so dynamic glyphs do not disturb the cluster spacing.
- 2026-05-29: =modules/media/MediaAnalyzer.qml= now shares the dotted-column visual language with the network graph while keeping a single upward spectrum. Live CAVA samples drive the graph when available, and the fake animation remains only as a no-sample fallback.
- 2026-05-29: QML project metadata stays portable through =config/quickshell/qmldir=. NVF stays generic and only supplies Qt/QML tooling plus generic QML project-root markers. Generated =.qmlls.ini= files remain ignored because they can contain machine-local Quickshell build paths.
- 2026-05-30: Native wallpaper management started inside Quickshell while temporarily keeping the existing =awww= fallback. =root/Wallpaper.qml= owns the per-screen background layer, =root/WallpaperPicker.qml= owns the selector overlay surface, and =modules/wallpaper/= owns the bar icon, selector UI, file list, current wallpaper state, and state persistence. The current wallpaper is stored at =~/.local/state/quickshell/wallpaper.json= unless =XDG_STATE_HOME= points elsewhere. The default wallpaper directory is =$HOME/Projects/nixos-dotfiles/wallpapers= and can be overridden with =SKY_WALLPAPER_DIR=.
- 2026-05-30: Wallpaper geometry checkpoint passed after setting =WlrLayershell.exclusionMode: ExclusionMode.Ignore= on =root/Wallpaper.qml=. Hyprland reports =qs-wallpaper= at =y=0= with full monitor height on both displays, so the native wallpaper now renders underneath the acrylic bar instead of starting below the bar's reserved 36px area.
- 2026-05-30: Wallpaper selector polish added search/filter, a result count, a clearer current-wallpaper badge, and keyboard focus only while the picker is open. Per-monitor wallpaper state is intentionally deferred until it is actually needed.
- 2026-05-30: Native wallpaper cutover removed =awww= package/startup/service wiring in the cleanup branch. Quickshell now owns the wallpaper layer; =awww= remains only in historical docs/reports unless it is intentionally reintroduced. Final validation is an =nrs= plus reboot check.
- 2026-05-30: Theme/wallpaper bar action spacing was tightened by narrowing only =modules/theme/ThemeToggle.qml=. The wallpaper button, tray spacing, and status cluster geometry were intentionally left alone. Further pixel polish can happen later, but this is good enough for the launcher/clipboard resurrection checkpoint.
- 2026-05-30: Native launcher resurrection started. =root/Launcher.qml= owns the overlay layer, focus policy, screen targeting, signal-file toggle, and click-out mask. =modules/launcher/LauncherView.qml= owns app loading, fuzzy scoring, keyboard navigation, row rendering, and activation. =scripts/launcher-toggle= now signals the native overlay; the old fuzzel launcher remained temporarily as a fallback during testing.
- 2026-05-30: Native launcher results are apps-only by default. The old open-window results were removed to reduce noise; workspace/window navigation remains outside the launcher unless a future explicit window-search mode earns its place.
- 2026-05-30: Launcher app discovery uses desktop entries plus =modules/launcher/apps.json= for terminal-first tools that do not provide useful desktop files. Current custom entries include =BlueTUI=, =WireMix=, and =Neovim=.
- 2026-05-30: Native clipboard resurrection started. =root/Clipboard.qml= owns the overlay layer, focus policy, screen targeting, signal-file toggle, and click-out mask. =modules/clipboard/ClipboardView.qml= owns =cliphist list= loading, text filtering, keyboard navigation, copy-back through =wl-copy=, and item deletion. =scripts/clipboard-toggle= now signals the native overlay; the old fuzzel picker remained temporarily as a fallback during testing. Image preview was deferred until text history became boring and reliable.

** Test Notes
:PROPERTIES:
:CUSTOM_ID: test-notes
:END:
- 2026-05-17: Workspace split tested live. Workspaces render, update, and switch using Lua Hyprland dispatch. The visible button hitboxes match the interactive area.
- 2026-05-17: Window title split tested live. Title updates across window focus and workspace changes, with no visible module interference.
- 2026-05-17: Media split tested live. Media pill appears and collapses with active player state, play/pause works, prev/next controls work, and the duplicate =media= IPC handler warning is gone after restart. Remaining Firefox MPRIS =Position= DBus warnings appear to be player-side churn, not shell structure breakage.
- 2026-05-17: Status cluster split tested live. Configuration loads cleanly after restart and status behavior appears intact: clock/status rendering, network, bluetooth, volume, battery, and app-launch clicks continue to work. Polling remains unchanged from the pre-split implementation.
- 2026-05-17: Status polling split tested live after =nrs=. Quickshell reloads cleanly with =Configuration Loaded=, and the latest journal output shows no new status errors. Historical media/import warnings in the same journal tail were from earlier iterations, not the polling split.
- 2026-05-17: Fixed-width network traffic labels tested live after reload. Quickshell reports =Configuration Loaded= with no new warnings from the formatter-only change.
- 2026-05-17: Tuned status traffic width tested live after build/reload. The 340px pill and 80px network field are good for now, with clean reload and build results.
- 2026-05-17: OSD view split tested live. Volume, brightness, and battery OSD behavior passed, and Quickshell reloads cleanly with =Configuration Loaded=.
- 2026-05-17: Root surface migration tested live after restart. Bar and OSD behavior stayed consistent, and Quickshell starts cleanly with =Configuration Loaded=.
- 2026-05-17: Tray extraction tested live. The tray hides when empty, Quickshell loads cleanly, and plain activation works for Vesktop. Steam exposed that some tray clients need native menu handling, so right-click/menu-only support was added as a follow-up. Native StatusNotifier menus require =//@ pragma UseQApplication= at the top of =shell.qml=; without it Quickshell logs =Cannot display PlatformMenuEntry=. After a full Quickshell restart, the native menu opens and this slice is considered operational. Follow-up polish: tighten the tray menu hitbox/anchor and decide whether native menu styling is acceptable or should be replaced later by a themed QML menu.
- 2026-05-17: Launcher button extraction tested live after =nrs= and full Quickshell restart. The service starts cleanly with =Configuration Loaded=, and the slice is considered operational.
- 2026-05-27: Bar frame extraction tested live. Quickshell starts with =Configuration Loaded=; =ScreenFrame.qml= was removed, and the frame is now contained in =common/BarFrame.qml= while the input mask remains limited to =barRow=.
- 2026-05-17: Status clock extraction tested live. Quickshell reports =Configuration Loaded=, the clock renders as expected, and no visible anomalies were detected.
- 2026-05-17: Status bluetooth extraction tested live. Quickshell reports =Configuration Loaded=, the bluetooth status area behaves as expected, and no visible anomalies were detected. Follow-up polish: status click targets currently use the default cursor; add explicit pointer cursors and re-check hitbox bounds as a separate affordance pass. Refresh status icons in the same pass so network, bluetooth, volume, battery, and clock read as one coherent set. Cursor shape alone is not proof of hitbox size because =MouseArea= defaults to the normal arrow cursor.
- 2026-05-17: Status volume extraction tested live. Quickshell reports =Configuration Loaded=, the volume icon behaves as expected, and no visible anomalies were detected.
- 2026-05-17: Status battery extraction tested live. Quickshell reports =Configuration Loaded=, the battery indicator behaves as expected, and no visible anomalies were detected. Follow-up improvement: consider replacing timer-based status polling with event-driven or more real-time sources where Quickshell/DBus/system APIs make that reliable.
- 2026-05-17: Status network extraction tested live. Quickshell reports =Configuration Loaded=, network/traffic rendering behaves as expected, and no visible anomalies were detected.
- 2026-05-27: Live CAVA media analyzer tested after =nrs=; =cava 0.10.7= is on the NixOS system path, Quickshell starts cleanly, and the user confirmed the live media graph is up and running.
- 2026-05-27: =wlctl 0.1.6= binary package built and ran on the NixOS host. =nix eval --impure .#nixosConfigurations.nixos.config.environment.systemPackages --apply builtins.length= returned =206= after adding the package and launcher fallback.
- 2026-05-30: Native wallpaper manager live test passed on NixOS. Quickshell restarted cleanly, the wallpaper selector opens from the bar icon, static wallpapers render, animated GIF wallpapers render, static/animated switching works in both directions, and state persists in =~/.local/state/quickshell/wallpaper.json=.
- 2026-05-30: Native wallpaper full-screen layer test passed. =hyprctl layers -j= reports =qs-wallpaper= at =y=0= and full monitor height on both HDMI-A-1 and eDP-1 after Quickshell restart.
- 2026-05-30: Native launcher first slice live-tested on NixOS. Quickshell restarts with =Configuration Loaded=, =scripts/launcher-toggle= opens the =qs-launcher= overlay, the layer appears on the focused monitor, a second toggle closes it, and no new journal errors were produced by the launcher slice. User-facing typing/app activation still needs hands-on testing before replacing the clipboard path.
- 2026-05-30: Native launcher hand test passed. The apps-only launcher opens from the normal bind, searches app/custom entries, navigates with arrows and =Ctrl-j/k=, launches apps with =Enter=, closes cleanly, and the borderless blurred panel is visually approved.
- 2026-05-30: Native clipboard first slice live-tested on NixOS. Quickshell restarts with =Configuration Loaded=, =scripts/clipboard-toggle= opens the =qs-clipboard= overlay, the layer appears in =hyprctl layers -j=, a second toggle closes it, and no new journal errors were produced by the clipboard slice. User-facing history selection, deletion, and fallback testing still need hands-on confirmation.
- 2026-05-30: Native clipboard hand test passed for the core close/restore path. Click-out close and =Esc= close are clean, the picker feels responsive, and selecting a history entry returns focus to the previous window as intended.
- 2026-05-30: Native clipboard mechanics approved. Search/filtering, row navigation, selection/copy focus restore, deletion, =Esc= close, and click-out close all behave as intended.
- 2026-05-30: Native clipboard image preview added. Image entries are detected from =cliphist list= binary markers, use a photo glyph in the result row, and generate a selected-entry thumbnail under =$XDG_RUNTIME_DIR= through =cliphist decode | magick=. =system/modules/clipboard.nix= now owns =imagemagick= explicitly so the preview path does not depend on unrelated Emacs packages.
- 2026-05-30: Clipboard image preview and wallpaper selector polish passed user hand/visual testing. Per-monitor wallpaper state is not needed right now and stays on the longer-term todo list instead of complicating the current picker.
- 2026-05-30: =awww= cutover branch build check passed on NixOS. The system toplevel builds, evaluated Home Manager packages no longer include =awww=, and evaluated user services no longer include =awww-daemon=. Final validation still requires applying with =nrs= and rebooting.
- 2026-05-30: =awww= cutover validated after reboot. Wallpaper persisted through reboot, Quickshell starts cleanly, =awww-daemon.service= is gone, and no =awww= process is running.
- 2026-05-30: Fuzzel launcher/clipboard fallback cutover removed the fallback scripts, =fuzzel= package, =config/fuzzel= live link, generated =fuzzel.ini= output, and Hyprland =fuzzel= layer rule. Native Quickshell launcher and clipboard are now the only configured picker surfaces.
- 2026-05-30: Quickshell structure/QML metadata follow-up kept the runtime tree intact, updated the documented tree to match reality, and added =pragma ComponentBehavior: Bound= to delegate-heavy launcher, clipboard, and tray modules. =qmldir= remains the only tracked QML metadata file.
- 2026-07-19: Cleanup Slice 0A added the exact-source validation harness. Slice 0B adopts the audit's four-space Qt formatting policy for the five previously drifting QML files and aligns both clean-Emacs QML modes and their explicit formatter with that policy; Neovim already uses the four-space Qt default. The dated audit's Doom alignment item is historical because Doom is retired. These candidate source changes remain isolated from the live checkout until separately authorized.
- 2026-07-19: Cleanup Slice 1A candidate replaces three layout-owned =height= bindings with explicit =Layout.preferredHeight= constraints and removes four import-aware =qmllint= unused imports. Exact isolated MSI source validation reduces actionable diagnostics from 24 to 17: layout and unused-import findings are zero, all remaining findings are =unqualified=, and the 38 narrowly named tooling diagnostics are unchanged. The strict lint gate therefore remains intentionally red; this candidate has not been made live or runtime-hand-tested.
- 2026-07-19: Cleanup Slice 1B candidate gives the launcher and clipboard result delegates explicit IDs and qualifies all 17 parent-delegate =modelData=/=index= accesses, including the four tray accesses through its existing =trayIcon= ID. Exact isolated MSI source validation reduces actionable diagnostics from 17 to zero and passes the strict lint gate; the 38 narrowly named tooling diagnostics are unchanged. This lexical cleanup preserves the current delegate models and interaction paths. No Nix build, live checkout update, Quickshell reload, runtime verification, or hand test has been performed.
- 2026-07-19: Cleanup Slices 0A-1B are now live-verified on the MSI from immutable activation head =84bc1b96f465f4f59634372f19a27ebabefce39d=.  Exact-source validation, the separate pure no-link build, controlled source-live checkout activation, automated runtime assertions, available Quickshell hand tests, restored-state soak, and the separate clean-Emacs QML test passed while generation 860 and both system outputs stayed unchanged.  Current evidence is limited to connected output =eDP-1=: dual-output behavior remains unverified, no safe tray middle action was available, and aggregate acceptance plus promotion remain separate decisions.
- 2026-07-19: Performance follow-up remains open.  The first candidate process recorded a =1.1G= systemd peak during the complete interactive test sequence, while the stable final process was about 130-136 MiB current with a 142-146 MiB peak.  This is not proof of a leak; measure image-heavy overlays, especially the wallpaper picker, under controlled action-labeled runs before changing code.
- 2026-07-19: At =21:51:32-04:00= the user explicitly accepted Cleanup Slices 0A-1B as the known-good source-live checkpoint on =eDP-1= and accepted deferral of dual-output validation until an external display is available.  The acceptance-evidence commit/push, promotion to =main=, any live branch-name change, and later optimization slices remain separate decisions.
- 2026-07-19: The separately authorized acceptance-evidence commit was published and remote-verified at =41ba6f4d891374e18b002a777e86414a8076bfca=.  Promotion to =main= remains separately authorized work; promotion itself must not switch the live checkout, restart services, activate NixOS, or change the tested payload trees.
- 2026-07-19: A completion snapshot at =22:18:51-04:00= verified that the user-authorized promotion had fast-forwarded =main= from =73e2bd82003e21c7955a9c9d12c4b80ab86eb634= to accepted head =8d8848739e44f27af253f4092333fbbe243f7ce7= in a disposable separate worktree.  GitHub then resolved both =main= and =work/quickshell-cleanup= to that exact head, and the temporary worktree was removed.  The live MSI checkout remained clean at activation head =84bc1b96f465f4f59634372f19a27ebabefce39d= with the exact accepted QML, Emacs, and scripts trees; generation 860 and all three services remained unchanged and healthy.  This completion record is documentation-only; publishing it on the cleanup branch does not authorize a second =main= update or live-checkout synchronization.
- 2026-07-19: After separate authorization, the live cleanup checkout fast-forwarded documentation-only from =84bc1b96f465f4f59634372f19a27ebabefce39d= to =fb71bd382ae1a14991cdbfe9bc7f611027b0dbb5=.  The accepted QML, Emacs, and scripts trees, generation 860, and all three service PIDs remained unchanged.  A following read-only audit confirmed that cleanup 0A-1B did not complete the original audit's whole Slice 1: wallpaper shell safety, CAVA temp/config lifecycle, overlay timers, title/network state, clipboard preview/delete handling, and CAVA freshness remain proposed work.  No implementation is authorized by that classification.
- 2026-07-20: At the source-only stage, Cleanup Slice 1C had an uncommitted candidate.  Wallpaper list/load use direct process argv, while the module-private =wallpaper-io= helper receives state and thumbnail values only as positional arguments.  It validates state JSON, publishes state and thumbnails through same-directory temporary files plus atomic rename, updates the watched signal path in place only after state publication, and lets the selector reload thumbnails only after helper success.  The isolated MSI source checker passed all 31 QML, 17 Bash, 2 JSON, and 1 Python files with no actionable lint diagnostics; targeted ShellCheck passed; disposable hostile-path, valid-prior pre-publication failure, cache-hit, signal-order, signal-write-failure, and real-FFmpeg fixtures passed.  Failure before a state rename preserves the prior valid state and signal; thumbnail generation or publication failure preserves the prior decodable JPEG.  A later signal-write failure can return nonzero after the individually atomic state file is already committed, so state and signal are ordered but are not a two-file transaction.  Those source checks did not update the live checkout, reload or activate anything, run a live interaction test, commit, or push.  Newline-containing wallpaper names, leading/trailing-whitespace filename fidelity, concurrent-writer locking, hard-kill temporary cleanup, directory =fsync= durability, and cross-file state/signal rollback remain outside this slice.
- 2026-07-20: After separate authorization, the Slice 1C pure no-link build gate passed in a fresh disposable MSI tree.  It returned the already-active =/nix/store/4a502xwqahv4q20niz34215l1iypp2zy-nixos-system-nixos-26.11.20260718.61b7c44= closure, left =flake.lock= unchanged, and created no =result= link.  This is expected because the live Quickshell tree is an out-of-store link: the build proves Nix evaluation and the system closure, while the separate exact-source checker proves the candidate QML and helper bytes.  The live checkout remained clean at =fb71bd382ae1a14991cdbfe9bc7f611027b0dbb5= with QML tree =2f9d5a4f3ccdf6bed1635d8ff5bb4668be741c98=; generation 860 and the Quickshell, Mako, and Emacs PIDs stayed unchanged.  No live-checkout update, activation, restart, runtime or hand test, commit, or push occurred.  This post-build documentation record was not itself part of the built disposable tree.
- 2026-07-20: Slice 1C is accepted source-live on =eDP-1= at immutable head =3e3d581ac0222b5c789e5ee2464b67f6128d7d98=.  The live checker, controlled stop/fast-forward/start, one clean configuration load, automated layer/state/service assertions, stability observations, and user hand result passed without changing generation 860.  The hand interaction republished the same valid wallpaper JSON as mode =0600= before updating the watched epoch signal about 5.6 ms later; no helper temporary remained.  The user explicitly deferred this candidate's fresh dual-output test until the second monitor is connected.  Exact evidence, rollback, claim boundaries, and remaining limitations are recorded in [[file:../handoffs/2026-07-20-quickshell-slice1c-acceptance.org][the Slice 1C acceptance handoff]].  The acceptance document was prepared separately from the runtime payload; its commit and push were later separately authorized and completed, as the following promotion record confirms.
- 2026-07-20: After separate commit, push, MSI documentation-sync, readiness, and promotion authorizations, GitHub =main= and =work/quickshell-cleanup= both resolved to documentation head =e4438b75de39ef1a2eb3d9d83cbac9bc94cefb0c=.  The promotion was a strict non-forced =0=-behind/=4=-ahead fast-forward from =8d8848739e44f27af253f4092333fbbe243f7ce7= in a disposable =main= worktree, which was removed after verification.  The live MSI checkout was already clean on the cleanup branch at =e4438b7= with the accepted QML, Emacs, and scripts trees; Quickshell PID =268275=, its start timestamp, =NRestarts=0=, generation 860, and the current system remained unchanged.  The inactive MSI local =main= and stale =origin/main= refs were intentionally untouched.  At this completion-record preparation snapshot, its own commit and push were separate pending gates, with Git and remote-ref evidence determining later completion.  Optional live synchronization, MSI ref reconciliation, dual-output testing, and Slice 1D remain separate decisions.
- 2026-07-22: Cleanup Slice 1D is accepted source-live on =eDP-1= at immutable payload =ae1736bfcf4d38fca501d14b19111137942b1386=, with acceptance evidence published at =89a725578c5d121560c76f1c2660112e1ed22fd1=.  The payload replaces shell-created =/tmp= configurations with tracked =modules/media/cava.conf= and direct CAVA argv.  Its exact-source and pure no-link build gates remained separate from controlled source activation; three typed IPC pause/play cycles, restored-state checks, and the user hand test passed with one direct-config CAVA while playing, none while paused, zero new matching CAVA temp configs, and generation 860 unchanged.  At the integration-readiness snapshot, the live checkout remained clean and detached at =ae1736b= while remote Slice 1D, cleanup, and =main= remained =89a7255=, =9377dba=, and =e4438b7= respectively.  No cleanup or =main= ref update followed from acceptance-evidence publication.  Dual-output validation, deletion of the 387 historical matches, analyzer freshness/exit handling, shared-analyzer work, and performance claims remain deferred.  Exact evidence and authority boundaries are in [[file:../handoffs/2026-07-22-quickshell-slice1d-acceptance.org][the Slice 1D acceptance handoff]].
- 2026-07-23: After separate readiness, local fast-forward, publication, and disposable-worktree cleanup gates, =main=, =work/quickshell-cleanup=, and =work/quickshell-slice1d= all resolved to accepted documentation head =89a2256efa701ab5c01e0c3caeddaa2801a345da=.  The live checkout remained clean and detached at payload =ae1736b= with the same QML tree, Quickshell PID, generation 860, and current system.  Promotion did not build or activate NixOS, restart services, delete historical configs, perform the deferred dual-output test, or authorize another optimization slice.
- 2026-07-23: Slice 1E is accepted and promoted at runtime payload =84b111b402cd34f42fd895725fde1810ee6e455c=.  Its only payload changes add =closeTimer.stop()= at the start of launcher and clipboard =show()=.  Exact-source validation and a separate pure no-link build passed before a controlled Quickshell source activation.  Automated live tests on =eDP-1= proved that both overlays preserve a typed sentinel across close/reopen within 150 ms, still clear state after an ordinary close, and finish closed.  Quickshell remained healthy with zero restarts or warning-level journal entries after activation; generation 860 and the current system did not change.  The branch was published, =main= was fast-forwarded and published separately, the live checkout was attached cleanly to =main=, and the clean Slice 1D and Slice 1E worktrees were removed while their branch refs were retained.  No NixOS activation, human hand test, historical CAVA cleanup, dual-output validation, Slice 1F implementation, or broader optimization was performed.
- 2026-07-24: Slice 1F is accepted source-live on =eDP-1= at payload =0022620328f32e847bf15afc9af473913858484c=.  Its only payload path is =config/quickshell/modules/clipboard/ClipboardView.qml=.  Exact-source and isolated-fixture gates, the separate pure no-link build, controlled source activation, automated synthetic preview/race and restored-state checks, service stability, and user acceptance passed without changing generation 860.  The user accepted the remote hand-equivalent evidence and waived a separate physical hand test.  The live UI Delete path was not exercised; cleanup removed only synthetic test rows, while all eight delete-selection cases passed in the isolated exact-component fixture.  The live checkout is clean and detached at the payload while local and remote =main= remain =7734eb4=.  Commit/push of this acceptance record, payload publication, promotion, worktree cleanup, dual-output validation, and later clipboard-lifecycle or optimization work remain separate decisions.  Exact evidence is in [[file:../handoffs/2026-07-24-quickshell-slice1f-acceptance.org][the Slice 1F acceptance handoff]].
- 2026-07-24: After separate evidence publication and readiness gates, local and GitHub =work/quickshell-cleanup= and =main= were strictly fast-forwarded through Slice 1F acceptance evidence =6ea87f52366719d3dbfb5f39d5f0312682074481=.  The live checkout then attached to =main= through a documentation-only delta; its Quickshell tree remained exactly equal to runtime payload =0022620328f32e847bf15afc9af473913858484c=.  Quickshell PID =4204=, =NRestarts=0=, generation 860, and the current system remained unchanged.  The clean payload and documentation worktrees were removed while their published branch refs were retained.  The commit containing this closeout record is documentation-only.  Aligning and publishing =main= and =work/quickshell-cleanup= at that commit closes the record without another documentation commit or any runtime, acceptance, or promotion gate.  Slice 1G must start from that final head; branch/worktree preparation alone includes no implementation.
- 2026-07-25: Slice 1G is accepted source-live on =eDP-1= at payload =e8358af1c6d6171d32f4bf51f1ca652c9a130d99=.  Its only payload path is =config/quickshell/modules/window/WindowTitle.qml=.  The rollback reproduced the background-close clear; the candidate passed exact-source checks, eight isolated event/query scenarios, the separate pure no-link build, controlled source activation, an automated live A/B close sequence, restored-state checks, and the repeated human visual sequence without changing generation 860.  The live checkout is clean and detached at the payload while local and remote =main= remain =95c0c49=.  Payload/evidence publication, promotion, live attachment, worktree cleanup, dual-output validation, native/shared-controller migration, and Slice 1H remain separate decisions.  Exact evidence is in [[file:../handoffs/2026-07-25-quickshell-slice1g-acceptance.org][the Slice 1G acceptance handoff]].
- 2026-07-25: After evidence publication and a fresh linear-readiness check, local and GitHub =work/quickshell-cleanup= and =main= were strictly fast-forwarded through Slice 1G acceptance evidence =180d7c2b8762d2f358fa39391e5a58724cfb272c=.  The live checkout then attached to =main= through only the five acceptance-documentation paths; its Quickshell tree remained identical to runtime payload =e8358af1c6d6171d32f4bf51f1ca652c9a130d99=.  Quickshell remained PID =104022= with =NRestarts=0=, and generation 860 stayed unchanged.  The clean payload and documentation worktrees were removed while their published refs were retained.  This closeout commit changes documentation only; aligning and publishing =main= and =work/quickshell-cleanup= at it closes the record without another runtime action.  Slice 1H is not authorized.
- 2026-07-25: Slice 1H network-handoff correctness is accepted and promoted at payload =ead19fe5ef2f986e60a3f956ada7b0c30576b94c=.  Its sole payload path is =config/quickshell/modules/status/StatusCluster.qml=.  The rollback reproduced stale traffic state across a device change; the candidate passed 14 deterministic reset, stale-result, A-B-A, zero-baseline, and rate scenarios, the exact source checker, a separate pure no-link build, controlled source activation, and an offscreen live-source probe that observed =wlp4s0= with advancing sample history.  Quickshell restarted as PID =137488= with =NRestarts=0=, and generation 860 remained unchanged.  Real disconnect/reconnect and Wi-Fi/Ethernet handoff was not attempted because =wlp4s0= was the sole SSH/default route and =eno1= had no carrier; it remains explicitly deferred until a wired alternate route is available.  Default-route selection, native/shared ownership, dual-output validation, broader optimization, and Slice 1I remain separate.
- 2026-07-25: Slice 1I analyzer freshness is accepted and promoted at runtime payload =f7107cb545741f53c2ae588d405297ed705e4180= with immutable acceptance evidence =f8a4c90b1033fbf94bf42ef08a13a4700c90d05c=.  Its sole payload path is =config/quickshell/modules/media/MediaAnalyzer.qml=.  The rollback reproduced frozen live levels after both a controlled stall and exit; the payload passed 13 exact-component checks, exact-source validation, the separate pure no-link build, controlled source activation, automated pause/play and forced-exit lifecycle checks, and the user-accepted stall/resume/exit visual sequence on =eDP-1=.  Local and GitHub =work/quickshell-cleanup= and =main= strictly fast-forwarded through the acceptance evidence.  The live checkout attached from detached payload to =main= through exactly the two acceptance-documentation paths with Quickshell tree =a362984be1d5812afe811c91f6fac257c4fffac2= unchanged; PID =431950=, =NRestarts=0=, generation 860, and the current system remained unchanged.  Both clean Slice 1I worktrees were removed while their published payload and evidence refs were retained.  The commit containing this closeout record changes exactly five documentation paths; aligning and publishing =main= and =work/quickshell-cleanup= at that commit, removing the clean closeout worktree, and passing final ref/worktree verification closes the record without another runtime action.  Slice 0C profiling remains the only open remainder proposal and is not authorized here; fresh dual-output validation, restart policy, shared ownership, rendering, frame-rate, performance claims, and historical CAVA cleanup remain separate.
- 2026-07-25: Slice 0C's read-only profiling helper is accepted and published at =41095ab7bde35293deaa1ec30b8363f72ccf39c6=.  The three-path commit adds =scripts/quickshell-profile=, registers it with =scripts/check-quickshell=, and records its contract and evidence in the existing remainder audit.  The source checker and separate pure no-link build passed.  A bounded =current-state-no-actions= observation on =eDP-1= kept PID =431950=, its invocation, =NRestarts=0=, generation 860, one paused MPRIS player, the expected two Quickshell layers, and =387= historical CAVA remnants stable, with no warning-level journal entries.  The sample authorizes no performance conclusion.  Local and GitHub =main= and =work/quickshell-slice0c= published the accepted commit; the clean isolated worktree was removed while the branch ref was retained.  No activation, service action, UI action, media action, historical CAVA cleanup, dual-output validation, or broader optimization occurred.
- 2026-07-25: Slice 3A workspace sharing is accepted and promoted at =c9337c69dcdb36de6de2df6080144c3d9ac14c97=.  One root controller now owns occupied-workspace polling and refresh events while each output derives its active workspace from its own Hyprland monitor.  Exact-source validation, the separate pure no-link build, controlled two-output activation, two stable playing-media profiles, and the user visual check passed.  The managed live link was restored to =main= with one Quickshell instance and zero restarts.  This completes only the workspace portion of original Slice 3; status, title, CAVA, and clock sharing remain separate.  Exact evidence and claim boundaries are in [[file:../handoffs/2026-07-25-quickshell-slice3a-acceptance.org][the Slice 3A acceptance handoff]].
- 2026-07-25: Slice 3B active-window-title sharing is accepted and promoted at =846f8623919e27718848f59d095e5d14b1f336cd=.  One root controller now owns the existing title-event, close-debounce, revision, and refresh state while every output renders the same accepted title.  Exact-source checks, the separate pure no-link build, and the automated and captured two-output A/background-B/Desktop sequence passed.  The managed link was restored to =main= with zero restarts or warnings, and all temporary Slice 3B artifacts and refs were removed.  Shared status, CAVA, and clock ownership remain separate.  Exact evidence and boundaries are in [[file:../handoffs/2026-07-25-quickshell-slice3b-acceptance.org][the Slice 3B acceptance handoff]].
- 2026-07-26: Slice 3C shared status ownership is accepted and promoted at =acdf5fc78b69cd832184ee7f12d50acae0e79921=.  One root controller now owns the unchanged five status pollers, network state, and battery-transition notifications while both bars retain their existing views, clicks, and local clocks.  The exact backend move, strict 34-QML source gate, disposable shared-view/network/battery fixture, separate pure no-link build, matched two-output captures, and paired playing-media observations passed.  Sampled children fell from 19 to the two unchanged CAVA processes, but CPU varied upward, so this is an ownership/process-churn result rather than a CPU claim.  The managed =main= link is healthy with zero restarts or warnings, and temporary Slice 3C artifacts and refs were removed.  Shared CAVA and clock ownership remain separate.  Exact evidence and boundaries are in [[file:../handoffs/2026-07-26-quickshell-slice3c-acceptance.org][the Slice 3C acceptance handoff]].
- 2026-07-26: Slice 3D shared CAVA ownership is accepted and promoted at payload =1101924147840f4a68aea2197f4aebf31a95fede= with acceptance evidence =2a74d3dd9d7b1a0e47e8706bd418854d6bb4c448=.  The existing root media controller now owns one unchanged CAVA parser/freshness backend while each output retains its Canvas and fallback rendering.  The exact source move, strict checker, separate pure no-link build, controlled two-output activation, pause/play, stall/resume, forced-exit recovery, and captured visual gates passed.  Local and GitHub =main= strictly fast-forwarded through the evidence; the managed link is healthy at PID =352826= with one CAVA child, zero restarts, and no warnings.  Temporary captures, rollback marker, and candidate worktree were removed while the local and remote evidence refs were retained.  This is an ownership result, not a CPU/GPU claim; shared minute-aligned clock ownership remains separate.  Exact evidence and boundaries are in [[file:../handoffs/2026-07-26-quickshell-slice3d-acceptance.org][the Slice 3D acceptance handoff]].
- 2026-07-26: Slice 3E shared minute-aligned clock ownership is accepted and promoted at payload =69f9670ba51144b5468aedf48925134ce78343c2= with acceptance evidence =49b672adf53e7dd79a07179a1f5bdf0c67d313ea=.  The existing root status controller now owns the unchanged time/date formatting and one one-shot timer aligned to the next minute, while each output retains its exact clock layout and text rendering.  The strict source checker, separate pure no-link build, controlled two-output activation, and captured =07:41= to =07:42= synchronized rollover passed.  Local and GitHub =main= strictly fast-forwarded through the evidence; the Home Manager-managed link was restored to promoted =main= with Quickshell PID =389704=, one CAVA child, zero restarts, and no warnings.  At restoration verification both connected outputs retained one bar and one wallpaper; system profile generation 863 and the current output were unchanged.  The candidate worktree and temporary artifacts were removed while local and remote evidence refs were retained.  This completes original Slice 3 without making a CPU/GPU/power claim.  Exact evidence and boundaries are in [[file:../handoffs/2026-07-26-quickshell-slice3e-acceptance.org][the Slice 3E acceptance handoff]].
- 2026-07-26: Slice 4A native PipeWire volume and mute state is accepted and promoted at payload =5c55f7c6f0f6ab94afe82a0e1b93ae67d550c90e= with acceptance evidence =0281fd90709edffb98d2bf1d1cbe589a3abf0636=.  The one-file payload replaces the two-second Bash/=wpctl=/=awk= reader with one native default-sink tracker while preserving views, launchers, OSD scripts, and the other status sources.  Strict source checks, the separate pure no-link build, rollback-protected candidate activation, rendered zero-volume and mute transitions, exact audio restoration, strict promotion, and final managed-link activation passed.  This removes 5,400 theoretical executions per hour from the status budget without making a CPU, memory, battery, or power claim.  Candidate and baseline worktree cleanup later completed while the evidence refs were retained.  Exact evidence and boundaries are in [[file:../handoffs/2026-07-26-quickshell-slice4a-acceptance.org][the Slice 4A acceptance handoff]].
- 2026-07-26: Slice 4B native UPower battery state is accepted and promoted at payload =ecb6e7f46388966ec5da26a4ac48a6a1e15ee677= with acceptance evidence =68218a77dacdea9bc005998d41ef81fda21cef4d=.  The one-file payload replaces the ten-second Bash/two-=cat=/two-=head= battery reader with native display-device and on-battery bindings while preserving the existing view and transition OSD contract.  Strict source checks, an exact controller fixture, the separate pure no-link build, rollback-protected candidate activation, one physical unplug/replug event in each direction, strict non-forced promotion, and final managed-link activation passed.  Local and GitHub =main= strictly fast-forwarded through the evidence commit; Quickshell is live at PID =527178= with one =eDP-1= bar and wallpaper, zero restarts, and no warnings.  This removes 1,800 theoretical executions per hour, reducing the source-derived status budget from 8,880 to 7,080 without making a CPU, memory, battery-life, or power claim.  Candidate and detached rollback worktree cleanup completed while the evidence refs were retained.  Exact evidence and boundaries are in [[file:../handoffs/2026-07-26-quickshell-slice4b-acceptance.org][the Slice 4B acceptance handoff]].
- 2026-07-26: Slice 4E1 native workspace occupancy is accepted, published, and promoted at payload =f0ef9a1ec5d1a7f44120301f5299827d4c1009ce= with acceptance head =177a7fa9fafb4ade99e529f93eb7765ee9368a4f=.  The controller now derives occupancy from =Hyprland.workspaces= and each workspace's native =toplevels= model; the obsolete timer, raw-event refresh, helper process, parser, and click refresh are removed.  The strict source checker, separate pure no-link build, automatic out-of-store reload, exact-controller probe, open/move/empty-active/close sequence, restored client state, and user acceptance passed with Quickshell PID =675223= and zero restarts or warnings.  Local and remote =main= strictly fast-forwarded to the published acceptance head without changing the live checkout or restarting Quickshell.  Fresh dual-output validation and Slice 4E2 native title work remain separate.  Exact evidence is in [[file:../handoffs/2026-07-26-quickshell-slice4e1-acceptance.org][the Slice 4E1 handoff]].
- 2026-07-29: Slice 4E2 native active-title state is accepted, published, and promoted through acceptance head =27f525b2fef9192f065f43d24ebd2939e8631bf6=.  The one-file payload replaces the active-window query, parser, timer, and raw-event backend with =ToplevelManager.activeToplevel= while preserving the shared controller and visible title contract.  The exact source checker, cold-start observer, separate pure no-link build, automated close-order and XWayland gate, restored-state checks, and human visual sequence passed on =eDP-1=.  Live-accepted payload =bbd7445b2be15ac5f915b63a66ace67975f70ff4= and current-main-aligned payload =a467e8b98d14651aa52493dc64130e1294b9a1b8= have identical patch IDs and controller bytes.  Local and GitHub =main= strictly fast-forwarded to the acceptance head; the managed Home Manager link was restored to promoted =main= without a Quickshell restart or NixOS activation.  Clean temporary-worktree removal completes after this documentation-only closeout is published; fresh dual-output validation remains deferred.  Exact evidence is in [[file:../handoffs/2026-07-29-quickshell-slice4e2-acceptance.org][the Slice 4E2 handoff]].

** Known Lesson
:PROPERTIES:
:CUSTOM_ID: known-lesson
:END:
The HDMI/HDR flicker investigation currently points at per-output =awww= wallpaper behavior, not the shell alone. Even so, Quickshell layers have caused real focus, click, and redraw problems before, so new tools must be checked for phantom masks and oversized input regions before they are considered done.
