* Emacs
:PROPERTIES:
:CUSTOM_ID: emacs
:END:
Clean Emacs is the active MSI Emacs configuration. Doom remains in the repo as
frozen reference material while the clean config absorbs the useful behavior.

** Shape
:PROPERTIES:
:CUSTOM_ID: shape
:END:
- Nix installs Emacs and external support tools.
- Clean Emacs lives in [[../../config/emacs][config/emacs]] and is linked to =~/.config/emacs=.
- Doom lives in [[../../config/doom][config/doom]] and is linked to =~/.config/doom= for reference.
- Emacs owns Emacs packages, editor behavior, keymaps, completion, LSP client behavior, Org, Dired, terminal integration, and theme faces.
- Nix owns external tools, language servers, formatters, fonts, services, wrappers, and links.
- Projects own project-local metadata such as =.dir-locals.el= and language-server hints.

** Installed By Nix
:PROPERTIES:
:CUSTOM_ID: installed-by-nix
:END:
- =emacs-pgtk= from the flake's Nixpkgs pin.
- =emacs-sync= loads packages and builds the vterm native module.
- Core project tools: Git, ripgrep, fd, EditorConfig, tree-sitter.
- Rich document support: sqlite, gnutls, ImageMagick, zstd, unzip, pandoc, TeX Live Small, aspell English, and Harper.
- Language/tooling support: Nix, Lua, QML/Qt, Rust, C/C++, Python, Haskell, shell, Markdown, YAML, JSON, GLSL, and Emacs Lisp.

** Active Modules
:PROPERTIES:
:CUSTOM_ID: active-modules
:END:
- Evil and evil-collection for Vim-style editing in normal and special buffers.
- Vertico, Orderless, Marginalia, Embark, Consult, Corfu, Cape, Yasnippet.
- Org, notes, agenda, capture, and source-block helpers.
- Magit for Git.
- Dired for file management.
- vterm and eshell for terminal workflows.
- Doom modeline, Sky theme faces, terminal-frame colors, and relative line numbers.

** Local Behavior
:PROPERTIES:
:CUSTOM_ID: local-behavior
:END:
- =config/emacs/README.org= documents the active clean Emacs key surface and module layout.
- =C-h=, =C-j=, =C-k=, and =C-l= move focus between Emacs windows across normal, insert, special buffers, and vterm.
- =TAB= and =S-TAB= move buffers globally, but Org and snippets override them locally for their native behavior.
- Line numbers are relative in code buffers and disabled in prose, Dired, help, vterm, term, shell, and eshell buffers.
- Yes/no prompts accept =y= and =n= globally.
- =SPC h r r= reloads clean Emacs modules; =SPC h r t= reloads the Sky theme.
- Terminal Emacs frames receive the 256-color-safe Sky fallback palette from the old Doom terminal-frame section.

** Commands
:PROPERTIES:
:CUSTOM_ID: commands
:END:
After changing package/module state:

#+begin_src sh
emacs-sync
systemctl --user restart emacs.service
#+end_src

Daemon/service commands:

#+begin_src sh
systemctl --user status emacs.service
systemctl --user restart emacs.service
systemctl --user stop emacs.service
emacsclient --create-frame --alternate-editor=emacs
#+end_src

** Checkpoints
:PROPERTIES:
:CUSTOM_ID: checkpoints
:END:
- 2026-05-30: Doom Emacs experiment checkpoint: upstream Doom, repo-local live config, Evil mode, Org/Magit/Dired/vterm, QML/Lua/Nix/Rust/C tooling, inline diagnostics, readable modeline, dashboard logo, relative line numbers, repo-local spell dictionary, and Doom-owned =SPC w ...= window behavior.
- 2026-05-30: Sky theme checkpoint: native =sky-dark= and =sky-light= Doom themes, shared face definitions, and =SPC h r t= reload for runtime theme changes.
- 2026-05-31: Emacs daemon checkpoint: NixOS owns an =emacs.service= user daemon, launcher entry opens =doom-emacs= via =emacsclient=, and terminal clients are available through =doom-emacs-terminal=.
- 2026-06-05: Clean Emacs active checkpoint: =~/.config/emacs= points to =config/emacs=, Doom is frozen reference, clean Evil key layer is usable, Org navigation works, vterm/eshell are ported, Doom modeline is active, terminal frames use Sky fallback colors, and prompts accept =y/n=.

** TODO Next Passes
:PROPERTIES:
:CUSTOM_ID: todo-next-passes
:END:
#+begin_src text
  TODO Finish Doom muscle-memory key audit for file/project/search/help/buffer edge cases.
  TODO Tune completion across minibuffer, Corfu, snippets, LSP, and prose.
  TODO Polish Doom modeline and Sky theme faces until the UI looks intentional.
  TODO vterm and eshell polish - faces like we had in doom.
  TODO treesitter?
  TODO Make Org first-class: notes, agenda, capture templates, source blocks, navigation.
  TODO Sanity-check LSP per language: Nix, Lua, QML, Rust, C/C++, Python, Haskell, shell, Markdown.
  TODO Decide formatting policy and keep it explicit instead of surprise autoformat.
  TODO Keep documenting the clean Emacs layout and user-facing key surface as it changes.
#+end_src
