#+title: MSI Conservative Nix GC - 2026-07-19
#+startup: overview

* Status And Boundary
This is an =ACCEPTED= source, build, activation, and runtime slice.  Generation
=859= installs an MSI-only, age-based Nix garbage-collection timer in an
explicitly unarmed state and removes the unsafe manual
=nix-collect-garbage -d= alias.

The timer and service have not started, and this slice did not collect or
optimize the store, delete a profile or GC root, limit or clean boot entries, or
reboot.  The first actual GC, automatic timer arming, store optimization, and
boot-entry retention remain separate authorization gates.

* Verified Starting Point
The read-only live inventory ended at =2026-07-19 12:20 EDT=:

| Item | Value |
|------+-------|
| Host | =nixos= at =192.168.1.175= |
| Checkout | =/home/skydive420dz/Projects/nixos-dotfiles= |
| Branch | =work/nixos-maintenance= |
| Starting commit | =9a6f1505ac8e0a9f3291356dd1b908ff8b4cc888= |
| Current generation | =858= |
| Current output | =/nix/store/1ba341k08ccy8mz2a6dq1p6af0j0xc53-nixos-system-nixos-26.11.20260718.61b7c44= |
| Booted generation | =856= |
| Booted output | =/nix/store/8q4h4bp340ikcp5wzflpsswia7p12x0s-nixos-system-nixos-26.11.20260718.61b7c44= |
| Root filesystem | =31%= used, about =1.30 TB= available |
| Nix store allocated size | =95,950,934,016= bytes |
| System generations | =11=, generations =848-858= |

The store has grown since the July 18 audit, but the filesystem is not under
space or inode pressure.  An exact reclaimable-byte figure was deliberately not
claimed: overlapping closure sizes and filesystem-visible roots do not account
for every runtime root.  A non-mutating comparison placed the difference
between all valid paths and paths reachable from visible persistent roots at
=21,159,136,072= NAR bytes; that is only an upper bound, not a GC forecast.

No GC liveness, collection, optimization, root cleanup, profile deletion, or
bootloader mutation command was used to obtain this baseline.

* Generation Retention Contract
Every current system generation remains younger than 30 days.  After activation,
the roles are:

| Generation | Role | Release condition |
|------------+------+-------------------|
| =859= | accepted current system | replaced only by a separately built and accepted generation |
| =856= | booted fallback | retain until a later accepted generation passes a real reboot test |
| =858= | immediate pre-859 rollback | retain through this timer slice and its separately observed first GC |
| =857= | accepted pre-858 rollback | retain through the separately observed first GC under the earlier contract |
| =855= | historical pre-retirement state that restores Ollama configuration | no permanent protection; allow the rolling policy to age it out |
| =848-854= | ordinary system history | allow the rolling policy to remove profile generations only after they exceed 30 days |

The 30-day window is rolling retention, not permanent pinning.  Before a
protected generation approaches 30 days, its release condition must be met or
the timer must remain disabled while an explicit protection decision is made.

QMK remains an accepted system-wide capability and is not a GC target.

* Confirmed Defect
=home/modules/programs/shell-aliases.nix= defined:

#+begin_src sh
ncg='sudo nix-collect-garbage -d'
#+end_src

The =-d= operation deletes older profile generations before collecting, which
can erase expected rollback entries.  The candidate removes the alias.  It does
not replace it with another unprompted maintenance command.  A future manual
alias can be considered only after the timer policy and first observed run are
accepted.

The alias table is shared by the Home Manager outputs for both flake hosts, so
the safe alias removal is shared too.  Only the automatic timer policy is
MSI-specific.

* Candidate Policy
The policy lives in =hosts/msi/maintenance.nix= and is imported only by the MSI
host:

#+begin_src nix
{ lib, ... }:

{
  nix.gc = {
    automatic = true;
    dates = "Sun *-*-* 04:15:00";
    options = "--delete-older-than 30d";
    persistent = false;
    randomizedDelaySec = "45min";
  };

  systemd.timers.nix-gc.wantedBy = lib.mkForce [ ];
}
#+end_src

The timer's first nominal calendar event after authoring is Sunday, July 26,
2026 at 04:15 EDT with up to 45 minutes of randomized delay.  Normal systemd
timer accuracy also applies, so this is not a guaranteed exact endpoint.
=persistent = false= prevents activation from catching up the already-passed
July 19 window.

The =wantedBy= override is the first-run safety latch: the unit is built and can
be inspected, but activation does not enable or start it and no calendar event
is armed.  After the first GC is separately authorized and observed, removing
this override is its own source, build, and activation slice.

The pinned =nix-collect-garbage= manual confirms that
=--delete-older-than 30d= first applies age-based generation deletion to
multiple profiles it discovers in default, system, and per-user profile
locations.  It is not restricted to the NixOS system profile and can affect
profiles belonging to other users.  The live inventory currently found:

- the 11 NixOS system generations listed above;
- one current Home Manager GC root and no independent Home Manager generation
  history;
- one current root-channel generation;
- one current user-channel generation;
- no standalone user =profile-N-link= history.

The profile inventory must be repeated immediately before the first authorized
GC.  A newly discovered user or Home Manager history pauses that maintenance
gate until its retention ownership is decided.

This candidate intentionally excludes:

- =nix.optimise= and =auto-optimise-store=;
- a Lanzaboote or systemd-boot configuration limit;
- cleanup of dangling GC roots or temporary roots;
- cleanup of legacy boot entries, channels, caches, journals, or coredumps;
- automatic flake or system upgrades.

* Source, Evaluation, And Build Results
The source candidate is the clean commit:

=2b3c7f4ff1192dd2946a611326ecaefd3d47399b=

Commit =24f64fe= introduced the MSI policy, alias removal, and this handoff.
Review then caught that a normal =WantedBy= would arm the next scheduled GC
during activation.  Commit =2b3c7f4= adds the empty =wantedBy= safety latch
before the first build.  No unsafe candidate was built or activated.

Source validation passed on the MSI:

- =git diff --check=;
- =nixfmt --check= for all changed Nix files;
- parse checks for the changed modules;
- MSI =nix.gc.automatic = true= with the documented calendar, age, persistence,
  and delay values;
- MSI =systemd.timers.nix-gc.wantedBy = []=;
- AMD =nix.gc.automatic = false=;
- MSI =nix.optimise.automatic = false=;
- generated Zsh and Emacs Eshell aliases omit =ncg= while retaining pure =nrs=
  and =nrb=.

The pure no-link build passed:

| Item | Value |
|------+-------|
| Candidate derivation | =/nix/store/z5a8n504nxz3ca7lhrvdylg7q51q5rn1-nixos-system-nixos-26.11.20260718.61b7c44.drv= |
| Candidate output | =/nix/store/3947ayk07qq4bfs948yrxk15spbgdidx-nixos-system-nixos-26.11.20260718.61b7c44= |
| Active closure | =31,186,122,768= bytes |
| Candidate closure | =31,186,123,360= bytes |
| Closure delta | =+592= bytes |

Direct inspection of the output proves:

- =nix-gc.service= is a one-shot unit whose script executes
  =nix-collect-garbage --delete-older-than 30d=;
- =nix-gc.timer= contains the intended =OnCalendar=, =Persistent=false=, and
  =RandomizedDelaySec=45min= values;
- the timer has no =[Install]= section and the candidate contains no
  =timers.target.wants/nix-gc.timer= link;
- =systemd-analyze calendar= reports the next nominal event as July 26 at 04:15
  EDT, but it is not armed;
- unit verification reports only the pre-existing NVIDIA
  =/var/run/nvidia-persistenced= PID-file warning, unrelated to this slice.

* Post-Build Live Proof
At =2026-07-19 12:40 EDT=:

- =/run/current-system= and the system profile still resolve to generation
  =858=;
- =/run/booted-system= still resolves to generation =856=;
- the current Home Manager root is unchanged;
- system profile links remain exactly generations =848-858=;
- the live system has no =nix-gc.timer= unit, the service is inactive, and its
  journal has no entries;
- the active Zsh still exposes the old =ncg= alias, independently proving that
  the candidate Home Manager output is not active;
- the ESP still contains 11 current UKIs and the same 50 stale legacy loader
  entries;
- the checkout is clean at =2b3c7f4=.

The build increased allocated store size from =96,616,161,280= to
=96,725,708,800= bytes and top-level store entries from =45,191= to =45,232=.
That =109,547,520=-byte increase is expected build material, not a collection.
This work ran no removal command; generation, profile, boot-entry, and visible
GC-root inventories remained unchanged.

* Activation And Runtime Verification
The user completed the separately authorized activation at approximately
=2026-07-19 12:52 EDT=.  Generation =859= is current:

=/nix/store/3947ayk07qq4bfs948yrxk15spbgdidx-nixos-system-nixos-26.11.20260718.61b7c44=

That is exactly the pure-evaluated and no-link-built output.  Runtime evidence:

- the system profile and =/run/current-system= resolve to generation =859=;
- =/run/booted-system= remains generation =856=;
- system profile links remain =848-858= and add only =859=;
- generation =858= is the immediate pre-activation rollback;
- the current Home Manager root is the build-verified
  =q303rnqzv1fgw8d6p1qvbn95vfyrkzyb-home-manager-generation=;
- the system state is =running= with no failed system or user units;
- the checkout is clean at documentation head =d48e995=, whose pure evaluation
  still returns the built output above.

The live GC unit state preserves the first-run safety contract:

| Property | Live value |
|----------+------------|
| Timer unit-file state | =linked= through the generation's =/etc= tree |
| Timer target enablement | absent from =timers.target.wants= and from the target's dependency list |
| Timer active state | =inactive (dead)= |
| Next trigger | none |
| Last trigger | none |
| Service active state | =inactive (dead)= |
| Service journal | no entries |

=linked= is not being used here to claim the timer is enabled.  NixOS links the
unit file into =/etc/systemd/system=, but the empty =wantedBy= list leaves it
unarmed: no target wants it and =systemctl list-timers --all= does not list it.
The service remains available only for the later, explicitly authorized manual
first run.

Alias and Home Manager verification passed:

- the live generated Zsh configuration and a fresh Zsh contain pure =nrs= and
  =nrb= aliases and no =ncg= alias;
- Home Manager deliberately stopped and started the Emacs daemon once because
  its generated alias file changed;
- the current daemon is =active (running)= with =NRestarts=0=,
  =Result=success=, and =ExecMainStatus=0=; =NRestarts=0= means systemd did not
  automatically restart this current invocation;
- the daemon loaded the candidate generated Eshell alias file, where =ncg= is
  absent and =nrs=/=nrb= remain pure;
- the system Home Manager activation service completed successfully.

Activation added the expected signed generation =859= UKI.  The ESP now has 12
current UKIs for generations =848-859= and retains the same 50 stale legacy
loader entries; no entry limit or cleanup occurred.  Visible GC roots increased
from 19 to 20 only because generation =859= gained its normal profile root.

At =12:55 EDT=, allocated store size was =96,763,015,168= bytes with =45,233=
top-level entries.  Root usage remains =31%= and =/boot= remains =19%= used.
These are post-activation baselines, not reclaimed-space results.

* User Acceptance
The user explicitly accepted generation =859= as the current known-good
activation checkpoint.  A final read-only check at =2026-07-19 13:01 EDT=
confirmed:

- generation =859= still matches the build-verified output;
- generation =856= remains booted fallback;
- the timer is linked but not target-enabled, inactive, and has no next or last
  trigger;
- the service remains inactive, has no activation timestamp, and has no journal
  entries;
- no failed system or user units were reported;
- the checkout was clean at =76b7268=.

Acceptance does not authorize a GC run or automatic timer arming.

* First GC Read-Only Preflight - Passed
** Boundary And Identity
The first-run inventory was refreshed between =2026-07-19 13:05= and =13:15
EDT= against:

| Item | Value |
|------+-------|
| Host | =nixos= at =192.168.1.175= |
| Nix | =2.34.8= |
| Checkout | =/home/skydive420dz/Projects/nixos-dotfiles= |
| Branch | =work/nixos-maintenance= |
| Head | clean =68313dd4e907c3df45166d2f66870eaeaab27f0a= |
| Current/system profile | accepted generation =859=, =3947ayk...= |
| Booted fallback | generation =856=, =8q4h4b...= |
| Immediate rollback | generation =858=, =1ba341...= |
| Earlier protected rollback | generation =857=, =drvm536...= |

The system is =running= with no failed system or user units.  The GC timer is
still linked but not target-enabled, inactive, and without a next or last
trigger.  The service is inactive, has no activation timestamp, and has no
journal entries.

No =nix-collect-garbage=, =nix-store --gc=, =nix store gc=, dry-run,
=--print-dead=, =--print-live=, =--print-roots=, optimization, cleanup, build,
activation, or reboot command was used.

** Profile-Generation Stage
The active service runs:

#+begin_src sh
nix-collect-garbage --delete-older-than 30d
#+end_src

Pinned Nix =2.34.8= first processes profile generations and then runs store
reachability GC.  Those phases are not transactional.

For a =30d= policy Nix retains:

- every generation created within 30 days;
- the newest generation already older than the cutoff;
- the current generation even when a profile has been rolled back.

At =2026-07-19 13:15 EDT= the preflight cutoff was
=2026-06-19 13:15 EDT=.  The collector recomputes its strict cutoff at actual
invocation time.  The refreshed profile inventory is:

| Profile | Generations | Age | Current | Expected age deletion |
|---------+-------------+-----+---------+-----------------------|
| NixOS system | =848-859= | =0.011-17.624d= | =859= | none |
| Root channels | only =channels-4-link= | =85.517d= | that sole generation | none |
| User XDG channels | only =channels-1-link= | =89.613d= | that sole generation | none |
| Home Manager | one =current-home= GC root; no independent generation history | current | =q303rn...= | none |
| User XDG profile | no =profile-N-link= history; selector absent | n/a | none | none |

Therefore no discovered non-current profile generation is older than 30 days,
and the expected profile-generation deletion count is zero.

With root's active =use-xdg-base-directories=false= setting, the collector scans
=/nix/var/nix/profiles=, its legacy per-user tree, root's profile directory, and
root's default-profile location.  It does not automatically enumerate every
modern =/home/*/.local/state/nix/profiles= directory.  The user XDG and Home
Manager locations were inventoried separately because they remain ownership and
rooting concerns.

The user completed the final privileged, read-only selector check:

| Selector | Confirmed state |
|----------+-----------------|
| =/root/.nix-profile= | =-> /nix/var/nix/profiles/per-user/root/profile=, timestamp =2026-04-17 21:23 EDT= |
| Root profile target | absent |
| =/root/.local/state/nix= | absent |

This confirms that root has no hidden profile generation history.  The expected
profile-generation deletion count is therefore zero across every discovered
profile.

Pinned Nix may create or refresh root's default-profile selector while
discovering profiles, even when its target is currently absent.  Repeat the
same privileged selector check after collection.  A selector timestamp or link
refresh would be collector bookkeeping, not evidence that a profile generation
was deleted.

** Persistent And Temporary Roots
Filesystem-visible persistent roots:

| Item | Count/state |
|------+-------------|
| Root symlinks | =20= |
| Resolving roots | =18= |
| Dangling roots | =2= |
| Normal new root | generation =859= |

The two dangling roots are:

- =gcroots/auto/dqss7ab... -> ~/.local/state/home-manager/gcroots/new-home=;
- =gcroots/profiles -> /mnt/nix/var/nix/profiles=.

Temporary-root state at =13:09 EDT=:

| Item | Value |
|------+-------|
| Files | =102= |
| Apparent bytes reported by =du -b= | =135,816,816= |
| Filenames with a live =/proc= PID | =0= |
| Filenames with a dead PID | =102= |

The first real GC is expected to remove dead temporary-root files and the
dangling indirect root under =gcroots/auto=.  Pinned Nix does not automatically
remove the dangling direct =gcroots/profiles= link.  These are expected
collector bookkeeping effects, not authorization for manual root cleanup.

The =/nix/var/nix/db/reserved= emergency file is present at =8,388,608= bytes.
GC removes that reserve before collection, and later store initialization may
recreate it.  Its bytes are outside the collector's reported freed-byte total,
so printed GC bytes and filesystem delta need not match.

** Store And Filesystem Before-State
At =13:07 EDT=:

| Item | Value |
|------+-------|
| Root/store filesystem used | =563,886,424,064= bytes, =31%= |
| Root/store filesystem available | =1,302,919,696,384= bytes |
| Root/store inode use | =3%= |
| Allocated =/nix/store= size | =96,800,325,632= bytes |
| Top-level store entries | =45,234= including =.links= |
| Valid store paths | =45,233= |
| All valid-path NAR bytes | =87,788,895,496= |
| Paths reachable from visible persistent roots | =6,977= |
| Visible-root-reachable NAR bytes | =65,868,689,536= |
| =/nix/store/.links= | zero files, =4,096= allocated bytes |
| Nix database | =108,834,816= bytes |
| Nix build logs | =71,233,536= bytes |

The difference between all valid paths and paths reachable from the visible
persistent roots is =38,256= paths and =21,920,205,960= NAR bytes, about
=20.4 GiB=.  This is only a conservative upper bound on candidate garbage.
Runtime roots, GC-time roots, new temporary roots, and NAR-versus-allocated
accounting prevent it from being an exact deletion list or reclaim forecast.
Disk and inode headroom remain ample, so there is no emergency pressure.

** Boot And Manifest Before-State
Secure Boot remains enabled.  Boot state is:

- 12 current Type-2 UKIs for generations =848-859=;
- generation =859= is the configured default;
- generation =856= is selected/booted;
- the same 50 stale Type-1 entries for generations =671-720= remain, and all
  still reference missing kernel, initrd, and system targets;
- =/boot= is =19%= used.

The GC does not update the ESP.  Boot cleanup and an entry limit remain separate
slices.

Internal comparison fingerprints for the observed run:

| Manifest | SHA-256 |
|----------+---------|
| System/root profile links | =f2a162f365a528988d8bf3abcea35d25d692c0333d82a070981cd8d0ec719fbd= |
| User XDG/Home Manager links | =f3a919125141c0a620e713df23ad98e1fb125ec2e9a1563a89dc0a783a60f95e= |
| Filesystem-visible GC-root links | =829de61c66e71d600920962b055430fc31a3ac0b73fd021daa9ccab07de9bf67= |
| Current UKIs | =911a63657a05906da55a91ca3ecf1e2020451df6491c9b0fe9d3a335cd0413d8= |
| Legacy loader entries | =f2e2828774f8f57d11d15c1e49d11a675d2405141b1afdc87419cd0c10b03290= |

Each fingerprint hashes a lexically sorted manifest of =path -> link target=
lines, or =filename size= lines for boot files.  They are quick drift checks;
the observed run must still retain the raw before/after inventories and report
their exact diff.

** Failure And Observation Contract
The profile-pruning and store-collection phases are not one transaction.  A
failure can occur after profile links, stale roots, or some store paths have
already changed.  Do not blindly retry a failed service; repeat the read-only
inventory first.

For a successful first run require all of:

1. =Result=success= and =ExecMainStatus=0=, not only the collector's final
   summary;
2. current generation =859=, booted generation =856=, and rollback generations
   =858= and =857= still present with their original store paths;
3. an exact before/after profile-link and root diff;
4. timer still inactive and unarmed with no next trigger;
5. before/after store allocation, path count, filesystem bytes/inodes, temporary
   roots, root's privileged profile selectors, reserve file, UKIs, loader
   entries, and failed units;
6. no optimization, timer arming, boot cleanup, manual root/profile deletion,
   activation, or reboot.

The read-only preflight passes.  Immediately before a later approved run,
capture one final cutoff, before-state, privileged root-selector snapshot, and
journal cursor.  The run still requires separate explicit authorization and
starts only =nix-gc.service= once.

* Verification Gates
** Source Gate
1. Confirm the diff contains only this handoff, the MSI import and policy, and
   removal of =ncg=.
2. Format and parse the changed Nix files.
3. Pure-evaluate MSI values and prove the guarded AMD output still has automatic
   GC disabled.
4. Prove the MSI timer has an empty =wantedBy= list.

** Build Gate
1. Run a pure, no-link MSI system build.
2. Inspect the candidate's generated =nix-gc.service= and =nix-gc.timer=.
3. Prove the service command contains =--delete-older-than 30d=.
4. Prove the timer uses the intended calendar, =Persistent=false=, and the
   45-minute randomized delay.
5. Prove the candidate has no =timers.target.wants/nix-gc.timer= link.
6. Recheck live generation, profiles, store usage, service history, and
   repository state to prove that building did not collect or activate.

** Activation Gate - Passed
The separately approved activation and runtime checks passed.  The timer is
linked into the active generation but not target-enabled, remains inactive, has
no next trigger, and has never run.

** User Acceptance Gate - Passed
The user explicitly accepted generation =859= as the current known-good
checkpoint after machine-verifiable activation checks passed.

** First Maintenance Gate - Not Authorized Here
The full read-only preflight passed.  The first actual GC still requires another
explicit approval and an observed manual start of the generated service while
the timer remains unarmed.  Record the full before/after contract above.
Automatic arming remains a later source/build/activation decision.

Store optimization and boot-entry limiting remain later independent slices.
