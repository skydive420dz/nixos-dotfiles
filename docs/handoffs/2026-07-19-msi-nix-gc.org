#+title: MSI Conservative Nix GC - 2026-07-19
#+startup: overview

* Status And Boundary
This records the =ACCEPTED= historical generation =859= source, build,
activation, and runtime slice and its =ACCEPTED= first observed maintenance
run.  Generation =859= installed an MSI-only, age-based Nix
garbage-collection timer in an explicitly unarmed state and removed the unsafe
manual =nix-collect-garbage -d= alias.

After the complete read-only preflight, the user separately authorized and
started exactly one =nix-gc.service= invocation.  It completed successfully
without deleting a profile generation or changing the accepted current,
booted, or protected rollback generations.  Under generation =859=, the timer
itself had never started and remained inactive and unarmed.

The observed run collected unreachable store paths and performed only its
expected stale-root bookkeeping.  It did not optimize the store, manually
delete a profile or GC root, limit or clean boot entries, activate a generation,
or reboot.

A later =ACCEPTED= source/build candidate at =e51f91a= removes only the
completed first-run safety latch.  The user separately authorized its exact
built output, which is now active as generation =860=.  The timer is enabled
and =active (waiting)= for its first trigger on =2026-07-26 04:56:30 EDT=.
Runtime checks pass, and the user accepted evidence checkpoint =f10e1ef= as the
active generation/timer checkpoint.  Store optimization and boot-entry
retention remain separate authorization gates.

* Verified Starting Point
The read-only live inventory ended at =2026-07-19 12:20 EDT=:

| Item | Value |
|------+-------|
| Host | =nixos= at =192.168.1.175= |
| Checkout | =/home/skydive420dz/Projects/nixos-dotfiles= |
| Branch | =work/nixos-maintenance= |
| Starting commit | =9a6f1505ac8e0a9f3291356dd1b908ff8b4cc888= |
| Current generation | =858= |
| Current output | =/nix/store/1ba341k08ccy8mz2a6dq1p6af0j0xc53-nixos-system-nixos-26.11.20260718.61b7c44= |
| Booted generation | =856= |
| Booted output | =/nix/store/8q4h4bp340ikcp5wzflpsswia7p12x0s-nixos-system-nixos-26.11.20260718.61b7c44= |
| Root filesystem | =31%= used, about =1.30 TB= available |
| Nix store allocated size | =95,950,934,016= bytes |
| System generations | =11=, generations =848-858= |

The store has grown since the July 18 audit, but the filesystem is not under
space or inode pressure.  An exact reclaimable-byte figure was deliberately not
claimed: overlapping closure sizes and filesystem-visible roots do not account
for every runtime root.  A non-mutating comparison placed the difference
between all valid paths and paths reachable from visible persistent roots at
=21,159,136,072= NAR bytes; that is only an upper bound, not a GC forecast.

No GC liveness, collection, optimization, root cleanup, profile deletion, or
bootloader mutation command was used to obtain this baseline.

* Generation Retention Contract
Every current system generation remains younger than 30 days.  After activation,
the roles are:

| Generation | Role | Release condition |
|------------+------+-------------------|
| =860= | accepted current timer-armed system | replace only through a separately built, activated, runtime-verified, and accepted generation |
| =859= | accepted immediate pre-860 rollback with unarmed timer | retain until generation 860 and its active timer are explicitly accepted |
| =856= | booted fallback | retain until a later accepted generation passes a real reboot test |
| =858= | immediate pre-859 rollback | retained by the completed first run; keep pending a later explicit retention decision |
| =857= | accepted pre-858 rollback | retained by the completed first run; future rolling-policy treatment remains a later decision |
| =855= | historical pre-retirement state that restores Ollama configuration | no permanent protection; allow the rolling policy to age it out |
| =848-854= | ordinary system history | allow the rolling policy to remove profile generations only after they exceed 30 days |

The 30-day window is rolling retention, not permanent pinning.  Before a
protected generation approaches 30 days, its release condition must be met or
the active timer must be separately disarmed while an explicit protection
decision is made.

QMK remains an accepted system-wide capability and is not a GC target.

* Confirmed Defect
=home/modules/programs/shell-aliases.nix= defined:

#+begin_src sh
ncg='sudo nix-collect-garbage -d'
#+end_src

The =-d= operation deletes older profile generations before collecting, which
can erase expected rollback entries.  The generation =859= slice removed the
alias.  It did not replace it with another unprompted maintenance command.  A
future manual alias can be considered only after the timer policy and first
observed run are accepted.

The alias table is shared by the Home Manager outputs for both flake hosts, so
the safe alias removal is shared too.  Only the automatic timer policy is
MSI-specific.

* Accepted Generation 859 Policy
The policy lives in =hosts/msi/maintenance.nix= and is imported only by the MSI
host:

#+begin_src nix
{ lib, ... }:

{
  nix.gc = {
    automatic = true;
    dates = "Sun *-*-* 04:15:00";
    options = "--delete-older-than 30d";
    persistent = false;
    randomizedDelaySec = "45min";
  };

  systemd.timers.nix-gc.wantedBy = lib.mkForce [ ];
}
#+end_src

The timer's first nominal calendar event after authoring is Sunday, July 26,
2026 at 04:15 EDT with up to 45 minutes of randomized delay.  Normal systemd
timer accuracy also applies, so this is not a guaranteed exact endpoint.
=persistent = false= prevents activation from catching up the already-passed
July 19 window.

The =wantedBy= override is the first-run safety latch: the unit is built and can
be inspected, but activation does not enable or start it and no calendar event
is armed.  The prerequisite first GC has now been separately observed.
Generation =859= contains this override and remains the accepted immediate
rollback.  The later generation =860= documented below activates the source
change that removes it.

The pinned =nix-collect-garbage= manual confirms that
=--delete-older-than 30d= first applies age-based generation deletion to
multiple profiles it discovers in default, system, and per-user profile
locations.  It is not restricted to the NixOS system profile and can affect
profiles belonging to other users.  The live inventory currently found:

- the 11 NixOS system generations listed above;
- one current Home Manager GC root and no independent Home Manager generation
  history;
- one current root-channel generation;
- one current user-channel generation;
- no standalone user =profile-N-link= history.

The profile inventory must be repeated immediately before the first authorized
GC.  A newly discovered user or Home Manager history pauses that maintenance
gate until its retention ownership is decided.

This accepted generation =859= slice intentionally excludes:

- =nix.optimise= and =auto-optimise-store=;
- a Lanzaboote or systemd-boot configuration limit;
- cleanup of dangling GC roots or temporary roots;
- cleanup of legacy boot entries, channels, caches, journals, or coredumps;
- automatic flake or system upgrades.

* Generation 859 Source, Evaluation, And Build Results
The source candidate is the clean commit:

=2b3c7f4ff1192dd2946a611326ecaefd3d47399b=

Commit =24f64fe= introduced the MSI policy, alias removal, and this handoff.
Review then caught that a normal =WantedBy= would arm the next scheduled GC
during activation.  Commit =2b3c7f4= adds the empty =wantedBy= safety latch
before the first build.  No unsafe candidate was built or activated.

Source validation passed on the MSI:

- =git diff --check=;
- =nixfmt --check= for all changed Nix files;
- parse checks for the changed modules;
- MSI =nix.gc.automatic = true= with the documented calendar, age, persistence,
  and delay values;
- MSI =systemd.timers.nix-gc.wantedBy = []=;
- AMD =nix.gc.automatic = false=;
- MSI =nix.optimise.automatic = false=;
- generated Zsh and Emacs Eshell aliases omit =ncg= while retaining pure =nrs=
  and =nrb=.

The pure no-link build passed:

| Item | Value |
|------+-------|
| Candidate derivation | =/nix/store/z5a8n504nxz3ca7lhrvdylg7q51q5rn1-nixos-system-nixos-26.11.20260718.61b7c44.drv= |
| Candidate output | =/nix/store/3947ayk07qq4bfs948yrxk15spbgdidx-nixos-system-nixos-26.11.20260718.61b7c44= |
| Active closure | =31,186,122,768= bytes |
| Candidate closure | =31,186,123,360= bytes |
| Closure delta | =+592= bytes |

Direct inspection of the output proves:

- =nix-gc.service= is a one-shot unit whose script executes
  =nix-collect-garbage --delete-older-than 30d=;
- =nix-gc.timer= contains the intended =OnCalendar=, =Persistent=false=, and
  =RandomizedDelaySec=45min= values;
- the timer has no =[Install]= section and the candidate contains no
  =timers.target.wants/nix-gc.timer= link;
- =systemd-analyze calendar= reports the next nominal event as July 26 at 04:15
  EDT, but it is not armed;
- unit verification reports only the pre-existing NVIDIA
  =/var/run/nvidia-persistenced= PID-file warning, unrelated to this slice.

* Generation 859 Post-Build Live Proof
At =2026-07-19 12:40 EDT=:

- =/run/current-system= and the system profile still resolve to generation
  =858=;
- =/run/booted-system= still resolves to generation =856=;
- the current Home Manager root is unchanged;
- system profile links remain exactly generations =848-858=;
- the live system has no =nix-gc.timer= unit, the service is inactive, and its
  journal has no entries;
- the active Zsh still exposes the old =ncg= alias, independently proving that
  the candidate Home Manager output is not active;
- the ESP still contains 11 current UKIs and the same 50 stale legacy loader
  entries;
- the checkout is clean at =2b3c7f4=.

The build increased allocated store size from =96,616,161,280= to
=96,725,708,800= bytes and top-level store entries from =45,191= to =45,232=.
That =109,547,520=-byte increase is expected build material, not a collection.
This work ran no removal command; generation, profile, boot-entry, and visible
GC-root inventories remained unchanged.

* Activation And Runtime Verification
The user completed the separately authorized activation at approximately
=2026-07-19 12:52 EDT=.  Generation =859= is current:

=/nix/store/3947ayk07qq4bfs948yrxk15spbgdidx-nixos-system-nixos-26.11.20260718.61b7c44=

That is exactly the pure-evaluated and no-link-built output.  Runtime evidence:

- the system profile and =/run/current-system= resolve to generation =859=;
- =/run/booted-system= remains generation =856=;
- system profile links remain =848-858= and add only =859=;
- generation =858= is the immediate pre-activation rollback;
- the current Home Manager root is the build-verified
  =q303rnqzv1fgw8d6p1qvbn95vfyrkzyb-home-manager-generation=;
- the system state is =running= with no failed system or user units;
- the checkout is clean at documentation head =d48e995=, whose pure evaluation
  still returns the built output above.

The live GC unit state preserves the first-run safety contract:

| Property | Live value |
|----------+------------|
| Timer unit-file state | =linked= through the generation's =/etc= tree |
| Timer target enablement | absent from =timers.target.wants= and from the target's dependency list |
| Timer active state | =inactive (dead)= |
| Next trigger | none |
| Last trigger | none |
| Service active state | =inactive (dead)= |
| Service journal | no entries |

=linked= is not being used here to claim the timer is enabled.  NixOS links the
unit file into =/etc/systemd/system=, but the empty =wantedBy= list leaves it
unarmed: no target wants it and =systemctl list-timers --all= does not list it.
The service remains available only for the later, explicitly authorized manual
first run.

Alias and Home Manager verification passed:

- the live generated Zsh configuration and a fresh Zsh contain pure =nrs= and
  =nrb= aliases and no =ncg= alias;
- Home Manager deliberately stopped and started the Emacs daemon once because
  its generated alias file changed;
- the current daemon is =active (running)= with =NRestarts=0=,
  =Result=success=, and =ExecMainStatus=0=; =NRestarts=0= means systemd did not
  automatically restart this current invocation;
- the daemon loaded the candidate generated Eshell alias file, where =ncg= is
  absent and =nrs=/=nrb= remain pure;
- the system Home Manager activation service completed successfully.

Activation added the expected signed generation =859= UKI.  The ESP now has 12
current UKIs for generations =848-859= and retains the same 50 stale legacy
loader entries; no entry limit or cleanup occurred.  Visible GC roots increased
from 19 to 20 only because generation =859= gained its normal profile root.

At =12:55 EDT=, allocated store size was =96,763,015,168= bytes with =45,233=
top-level entries.  Root usage remains =31%= and =/boot= remains =19%= used.
These are post-activation baselines, not reclaimed-space results.

* User Acceptance
The user explicitly accepted generation =859= as the current known-good
activation checkpoint.  A final read-only check at =2026-07-19 13:01 EDT=
confirmed:

- generation =859= still matches the build-verified output;
- generation =856= remains booted fallback;
- the timer is linked but not target-enabled, inactive, and has no next or last
  trigger;
- the service remains inactive, has no activation timestamp, and has no journal
  entries;
- no failed system or user units were reported;
- the checkout was clean at =76b7268=.

Acceptance does not authorize a GC run or automatic timer arming.

* First GC Read-Only Preflight - Passed
This section preserves the evidence and expectations sealed before the
separately authorized run.  The resulting observed-run record follows it.

** Boundary And Identity
The first-run inventory was refreshed between =2026-07-19 13:05= and =13:15
EDT= against:

| Item | Value |
|------+-------|
| Host | =nixos= at =192.168.1.175= |
| Nix | =2.34.8= |
| Checkout | =/home/skydive420dz/Projects/nixos-dotfiles= |
| Branch | =work/nixos-maintenance= |
| Head | clean =68313dd4e907c3df45166d2f66870eaeaab27f0a= |
| Current/system profile | accepted generation =859=, =3947ayk...= |
| Booted fallback | generation =856=, =8q4h4b...= |
| Immediate rollback | generation =858=, =1ba341...= |
| Earlier protected rollback | generation =857=, =drvm536...= |

The system is =running= with no failed system or user units.  The GC timer is
still linked but not target-enabled, inactive, and without a next or last
trigger.  The service is inactive, has no activation timestamp, and has no
journal entries.

No =nix-collect-garbage=, =nix-store --gc=, =nix store gc=, dry-run,
=--print-dead=, =--print-live=, =--print-roots=, optimization, cleanup, build,
activation, or reboot command was used.

** Profile-Generation Stage
The active service runs:

#+begin_src sh
nix-collect-garbage --delete-older-than 30d
#+end_src

Pinned Nix =2.34.8= first processes profile generations and then runs store
reachability GC.  Those phases are not transactional.

For a =30d= policy Nix retains:

- every generation created within 30 days;
- the newest generation already older than the cutoff;
- the current generation even when a profile has been rolled back.

At =2026-07-19 13:15 EDT= the preflight cutoff was
=2026-06-19 13:15 EDT=.  The collector recomputes its strict cutoff at actual
invocation time.  The refreshed profile inventory is:

| Profile | Generations | Age | Current | Expected age deletion |
|---------+-------------+-----+---------+-----------------------|
| NixOS system | =848-859= | =0.011-17.624d= | =859= | none |
| Root channels | only =channels-4-link= | =85.517d= | that sole generation | none |
| User XDG channels | only =channels-1-link= | =89.613d= | that sole generation | none |
| Home Manager | one =current-home= GC root; no independent generation history | current | =q303rn...= | none |
| User XDG profile | no =profile-N-link= history; selector absent | n/a | none | none |

Therefore no discovered non-current profile generation is older than 30 days,
and the expected profile-generation deletion count is zero.

With root's active =use-xdg-base-directories=false= setting, the collector scans
=/nix/var/nix/profiles=, its legacy per-user tree, root's profile directory, and
root's default-profile location.  It does not automatically enumerate every
modern =/home/*/.local/state/nix/profiles= directory.  The user XDG and Home
Manager locations were inventoried separately because they remain ownership and
rooting concerns.

The user completed the final privileged, read-only selector check:

| Selector | Confirmed state |
|----------+-----------------|
| =/root/.nix-profile= | =-> /nix/var/nix/profiles/per-user/root/profile=, timestamp =2026-04-17 21:23 EDT= |
| Root profile target | absent |
| =/root/.local/state/nix= | absent |

This confirms that root has no hidden profile generation history.  The expected
profile-generation deletion count is therefore zero across every discovered
profile.

Pinned Nix may create or refresh root's default-profile selector while
discovering profiles, even when its target is currently absent.  Repeat the
same privileged selector check after collection.  A selector timestamp or link
refresh would be collector bookkeeping, not evidence that a profile generation
was deleted.

** Persistent And Temporary Roots
Filesystem-visible persistent roots:

| Item | Count/state |
|------+-------------|
| Root symlinks | =20= |
| Resolving roots | =18= |
| Dangling roots | =2= |
| Normal new root | generation =859= |

The two dangling roots are:

- =gcroots/auto/dqss7ab... -> ~/.local/state/home-manager/gcroots/new-home=;
- =gcroots/profiles -> /mnt/nix/var/nix/profiles=.

Temporary-root state at =13:09 EDT=:

| Item | Value |
|------+-------|
| Files | =102= |
| Apparent bytes reported by =du -b= | =135,816,816= |
| Filenames with a live =/proc= PID | =0= |
| Filenames with a dead PID | =102= |

The first real GC is expected to remove dead temporary-root files and the
dangling indirect root under =gcroots/auto=.  Pinned Nix does not automatically
remove the dangling direct =gcroots/profiles= link.  These are expected
collector bookkeeping effects, not authorization for manual root cleanup.

The =/nix/var/nix/db/reserved= emergency file is present at =8,388,608= bytes.
GC removes that reserve before collection, and later store initialization may
recreate it.  Its bytes are outside the collector's reported freed-byte total,
so printed GC bytes and filesystem delta need not match.

** Store And Filesystem Before-State
At =13:07 EDT=:

| Item | Value |
|------+-------|
| Root/store filesystem used | =563,886,424,064= bytes, =31%= |
| Root/store filesystem available | =1,302,919,696,384= bytes |
| Root/store inode use | =3%= |
| Allocated =/nix/store= size | =96,800,325,632= bytes |
| Top-level store entries | =45,234= including =.links= |
| Valid store paths | =45,233= |
| All valid-path NAR bytes | =87,788,895,496= |
| Paths reachable from visible persistent roots | =6,977= |
| Visible-root-reachable NAR bytes | =65,868,689,536= |
| =/nix/store/.links= | zero files, =4,096= allocated bytes |
| Nix database | =108,834,816= bytes |
| Nix build logs | =71,233,536= bytes |

The difference between all valid paths and paths reachable from the visible
persistent roots is =38,256= paths and =21,920,205,960= NAR bytes, about
=20.4 GiB=.  This is only a conservative upper bound on candidate garbage.
Runtime roots, GC-time roots, new temporary roots, and NAR-versus-allocated
accounting prevent it from being an exact deletion list or reclaim forecast.
Disk and inode headroom remain ample, so there is no emergency pressure.

** Boot And Manifest Before-State
Secure Boot remains enabled.  Boot state is:

- 12 current Type-2 UKIs for generations =848-859=;
- generation =859= is the configured default;
- generation =856= is selected/booted;
- the same 50 stale Type-1 entries for generations =671-720= remain, and all
  still reference missing kernel, initrd, and system targets;
- =/boot= is =19%= used.

The GC does not update the ESP.  Boot cleanup and an entry limit remain separate
slices.

Internal comparison fingerprints for the observed run:

| Manifest | SHA-256 |
|----------+---------|
| System/root profile links | =f2a162f365a528988d8bf3abcea35d25d692c0333d82a070981cd8d0ec719fbd= |
| User XDG/Home Manager links | =f3a919125141c0a620e713df23ad98e1fb125ec2e9a1563a89dc0a783a60f95e= |
| Filesystem-visible GC-root links | =829de61c66e71d600920962b055430fc31a3ac0b73fd021daa9ccab07de9bf67= |
| Current UKIs | =911a63657a05906da55a91ca3ecf1e2020451df6491c9b0fe9d3a335cd0413d8= |
| Legacy loader entries | =f2e2828774f8f57d11d15c1e49d11a675d2405141b1afdc87419cd0c10b03290= |

Each fingerprint hashes a lexically sorted manifest of =path -> link target=
lines, or =filename size= lines for boot files.  They are quick drift checks;
the observed run must still retain the raw before/after inventories and report
their exact diff.

** Failure And Observation Contract
The profile-pruning and store-collection phases are not one transaction.  A
failure can occur after profile links, stale roots, or some store paths have
already changed.  Do not blindly retry a failed service; repeat the read-only
inventory first.

For a successful first run require all of:

1. =Result=success= and =ExecMainStatus=0=, not only the collector's final
   summary;
2. current generation =859=, booted generation =856=, and rollback generations
   =858= and =857= still present with their original store paths;
3. an exact before/after profile-link and root diff;
4. timer still inactive and unarmed with no next trigger;
5. before/after store allocation, path count, filesystem bytes/inodes, temporary
   roots, root's privileged profile selectors, reserve file, UKIs, loader
   entries, and failed units;
6. no optimization, timer arming, boot cleanup, manual root/profile deletion,
   activation, or reboot.

The read-only preflight passed.  Immediately before the separately approved
run, the final cutoff, before-state, privileged root-selector snapshot, and
journal cursor were captured.  The user then started only =nix-gc.service=
once.

* First Observed Manual GC - Accepted
** Authorization And Execution Boundary
The final before-state was sealed at =2026-07-19 13:28:46 EDT=.  The user then
explicitly authorized and personally ran:

#+begin_src sh
sudo systemctl start nix-gc.service
#+end_src

The retained journal proves exactly one invocation:

| Item | Observed value |
|------+----------------|
| Journal start | =2026-07-19 13:32:09.798990 EDT= |
| Collector summary | =2026-07-19 13:32:39.251394 EDT= |
| Final accounting line | =2026-07-19 13:32:39.269240 EDT= |
| Invocation ID | =46de90339aff4daba08917245140e323= |
| Collector PID | =148683= |
| Journal result | =Deactivated successfully= and =Finished Nix Garbage Collector= |
| End state | =inactive (dead)= |
| Runtime | =29.469s= wall, =27.913s= CPU |
| Peak memory | =271.3M= |

There is one start line, one successful deactivation line, one finish line, and
one final collector summary.  The journal contains =3,232= total unit lines:
=3,228= collector-process lines and four systemd manager lines.  It contains
=3,117= unique store-deletion lines, with no duplicates or journald suppression.
Anchored failure checks and the journal's =err..alert= priorities contain zero
error, abort, panic, fatal, or failure entries.

After the oneshot unit object unloaded, =systemctl show= reported
=Result=success=, =ExecMainCode=0=, =ExecMainStatus=0=, and =NRestarts=0= but
no longer retained its timestamps, PID, or invocation ID.  Those default-looking
post-unload fields are not used alone as execution proof.  The user's
=systemctl start= returned without an error, and the retained journal's exact
invocation ID, collector summary, successful deactivation, finish line, and
absence of high-priority failures are the authoritative result evidence.

The three profile messages are scan notices for the system profile and root
channels, not deletion notices.  The exact profile-generation deletion count
is zero.

** Generation, Profile, And Boot Preservation
The preservation contract passed:

- generation =859= remains current and the system profile still resolves to
  =3947ayk...=;
- generation =856= remains booted and selected at =8q4h4b...=;
- protected rollback generations =857= at =drvm536...= and =858= at
  =1ba341...= remain present;
- all system generation links =848-859= retain their original paths and
  timestamps;
- the system/root profile fingerprint remains =f2a162f...= and the user
  XDG/Home Manager fingerprint remains =f3a9191...=;
- the accepted Home Manager root remains =q303rn...=;
- all 12 UKIs and all 50 legacy loader entries retain their original manifest
  fingerprints, =911a636...= and =f2e2828...= respectively;
- generation =859= remains the configured boot default, generation =856=
  remains selected, Secure Boot remains enabled, and =/boot= remains =19%= used;
- no failed system or user unit appeared.

The required privileged after-check exactly matches the before-check:

| Root selector | Before and after |
|---------------+------------------|
| =/root/.nix-profile= | timestamp =2026-04-17T21:23:56.3839786370-0400=, pointing to =/nix/var/nix/profiles/per-user/root/profile= |
| Root profile target | absent |
| =/root/.local/state/nix= | absent |

No profile link was deleted, created, retargeted, or refreshed.

** Root And Collector Bookkeeping
Only the expected collector-owned bookkeeping occurred:

| Item | Before | After | Exact change |
|------+--------+-------+--------------|
| Persistent root links | =20= | =19= | one stale indirect root removed |
| Resolving persistent roots | =18= | =18= | none |
| Dangling persistent roots | =2= | =1= | =gcroots/auto/dqss7ab...= removed |
| Persistent-root fingerprint | =829de61...= | =46dd7ef...= | expected single-link diff |
| Temporary-root files | =102= | =0= | 102 dead-PID files removed |
| Temporary-root apparent bytes | =135,816,816= | =0= | =-135,816,816= |
| Emergency reserve | =8,388,608= bytes | absent | removed by GC before collection |

The dangling direct root
=/nix/var/nix/gcroots/profiles -> /mnt/nix/var/nix/profiles= remains, as
predicted.  No manual root cleanup was performed.  The emergency reserve was
not deliberately recreated during the after-check.

** Store And Filesystem Accounting
The closest sealed filesystem, store-allocation, and entry-count snapshots are
=13:28:46= and =13:34:22 EDT=.  The valid-path and NAR-byte before totals come
from the =13:07= preflight database inventory; their after totals were queried
later through SQLite in immutable read-only mode.  No Nix command was used for
that database query.

| Measure | Before | After | Exact change |
|---------+--------+-------+--------------|
| Top-level store entries, including =.links= | =45,234= | =42,117= | =-3,117= |
| Valid store paths | =45,233= | =42,116= | =-3,117= |
| Valid-path NAR bytes | =87,788,895,496= | =66,509,612,632= | =-21,279,282,864=, about =19.818 GiB= |
| Allocated =/nix/store= size | =96,800,325,632= | =71,840,714,752= | =-24,959,610,880=, about =23.245 GiB= |
| Filesystem used bytes | =563,892,441,088= | =538,790,141,952= | =-25,102,299,136=, about =23.378 GiB= |
| Filesystem available bytes | =1,302,913,679,360= | =1,328,015,978,496= | =+25,102,299,136= |
| Filesystem use | =31%= | =29%= | =-2= percentage points |
| Used inodes | =3,514,268= | =2,310,362= | =-1,203,906= |
| Inode use | =3%= | =2%= | =-1= percentage point |

The collector reported:

#+begin_example
3117 store paths deleted, 19.6 GiB freed
#+end_example

The =3,117= deletion lines, valid-path reduction, and top-level entry reduction
agree exactly.  The reported collector bytes, valid-path NAR delta, allocated
store delta, and filesystem delta are deliberately recorded as different
measures.  They need not match because they account differently for store-file
allocation, NAR sizes, links, database activity, the emergency reserve, and
ordinary filesystem activity around the snapshots.

** Result Boundary
After the run:

- =nix-gc.timer= remains =inactive (dead)= and =linked= only through the
  generation's =/etc= tree;
- it remains absent from =timers.target.wants= and the target dependency list;
- it has no next or last trigger and remains absent from =list-timers --all=;
- no second collection, optimization, activation, reboot, boot cleanup, or
  manual profile/root deletion occurred;
- the live checkout remained clean at pre-run documentation head =19fb371=.

The user explicitly accepted evidence checkpoint =cccf57a= as the first-GC
maintenance checkpoint.  At that checkpoint, the next policy decision was
whether to remove the timer's empty =wantedBy= safety latch.  The later
=e51f91a= candidate documented below has now passed its source, pure-evaluation,
and build gates.  The user accepted its evidence checkpoint =9d45149=;
activation remained separate at that checkpoint and was later separately
authorized.  Store optimization and boot-entry retention remain independent
later decisions.

* Generation 859 And First-GC Verification Gates - Passed
** Source Gate
1. Confirm the diff contains only this handoff, the MSI import and policy, and
   removal of =ncg=.
2. Format and parse the changed Nix files.
3. Pure-evaluate MSI values and prove the guarded AMD output still has automatic
   GC disabled.
4. Prove the MSI timer has an empty =wantedBy= list.

** Build Gate
1. Run a pure, no-link MSI system build.
2. Inspect the candidate's generated =nix-gc.service= and =nix-gc.timer=.
3. Prove the service command contains =--delete-older-than 30d=.
4. Prove the timer uses the intended calendar, =Persistent=false=, and the
   45-minute randomized delay.
5. Prove the candidate has no =timers.target.wants/nix-gc.timer= link.
6. Recheck live generation, profiles, store usage, service history, and
   repository state to prove that building did not collect or activate.

** Activation Gate - Passed
The separately approved activation and runtime checks passed.  The timer is
linked into the active generation but not target-enabled, remains inactive, has
no next trigger, and has never fired.  The later manual service invocation is
recorded separately above.

** User Acceptance Gate - Passed
The user explicitly accepted generation =859= as the current known-good
checkpoint after machine-verifiable activation checks passed.

** First Maintenance Execution Gate - Completed
The full read-only preflight passed, the user separately authorized exactly one
manual start of the generated service, and the complete before/after contract
passed.  The timer remained unarmed throughout.

** First Maintenance User Acceptance Gate - Passed
The user explicitly accepted evidence checkpoint =cccf57a= as the first-GC
maintenance checkpoint after reviewing the machine-verifiable result.

** Automatic Timer Arming Historical Boundary
Generation =859= did not authorize timer arming.  The later source/build
candidate below supersedes only that source/build boundary; it does not
authorize activation.  Activation was later separately authorized and is
recorded below.

Store optimization and boot-entry limiting remain later independent slices.

* Automatic Timer Arming Candidate - Accepted
** Source Boundary
Before this candidate, both clean checkouts were at accepted documentation head
=90f64428b1b956dc749de4d3ae598823ecb743f2=.  The actual host remained:

| Item | Live value |
|------+------------|
| Current/system profile | generation =859=, =3947ayk...= |
| Booted fallback | generation =856=, =8q4h4b...= |
| Timer | =linked=, =inactive (dead)=, unarmed |
| Target wants link | absent |
| Next/last trigger | none |
| Recorded GC service starts | exactly one |

Source commit =e51f91a0c1c983b4f3f3ffb22b6cbf90d13940d7= changes only
=hosts/msi/maintenance.nix=.  Its six deletions remove:

- the completed first-run =lib.mkForce [ ]= =wantedBy= safety latch;
- the now-stale latch comment;
- the module function header, which existed only to bind =lib= for that
  override.

The result restores the maintenance module to its original plain-attribute-set
shape while leaving every GC policy value byte-for-byte unchanged:

#+begin_src nix
{
  nix.gc = {
    automatic = true;
    dates = "Sun *-*-* 04:15:00";
    options = "--delete-older-than 30d";
    persistent = false;
    randomizedDelaySec = "45min";
  };
}
#+end_src

No explicit replacement =wantedBy= is added.  Its raw option default remains
empty.  Removing =mkForce [ ]= allows the non-empty MSI service =startAt= value,
synthesized from =nix.gc.automatic= and =nix.gc.dates=, to generate
=nix-gc.timer.wantedBy = [ "timers.target" ]=.  The commit does not change
=flake.lock=, aliases, optimization, profiles, GC roots, boot retention, or
another host.

** Source And Evaluation Gate - Passed
The following checks passed in the clean live-host checkout:

- =git diff --check=;
- =nixfmt --check hosts/msi/maintenance.nix=;
- =nix-instantiate --parse hosts/msi/maintenance.nix=;
- pure flake evaluation with =--no-write-lock-file=.

Pure evaluation produced:

| Policy | MSI =nixos= | AMD =desktop-amd= |
|--------+-------------+-------------------|
| =nix.gc.automatic= | =true= | =false= |
| GC options | =--delete-older-than 30d= | empty |
| Effective timer schedule | =Sun *-*-* 04:15:00= | none |
| Timer =wantedBy= | =timers.target= | timer absent |
| Service =startAt= | scheduled through the timer | empty |
| Service =wantedBy= | no independent target owner | empty |
| =nix.optimise.automatic= | =false= | =false= |

Pinned NixOS defines a manually invocable =nix-gc.service= whenever Nix is
enabled, so the AMD service attribute exists.  With
=nix.gc.automatic = false=, its =startAt= and =wantedBy= lists are empty and no
=nix-gc.timer= exists.  AMD's raw =nix.gc.dates= default is =03:15=, but it is
inert because automatic GC is disabled.  The MSI schedule and
=--delete-older-than 30d= option therefore do not leak into AMD.

** Pure No-Link Build Gate - Passed
The actual MSI host ran:

#+begin_src sh
nix --extra-experimental-features 'nix-command flakes' \
  build --print-out-paths \
  '.#nixosConfigurations."nixos".config.system.build.toplevel' \
  --no-link --no-write-lock-file --show-trace
#+end_src

It completed successfully:

| Item | Value |
|------+-------|
| Source commit | =e51f91a0c1c983b4f3f3ffb22b6cbf90d13940d7= |
| Derivation | =/nix/store/fqc1d3i6ba1qk7z6xbadvhlb8i77zc57-nixos-system-nixos-26.11.20260718.61b7c44.drv= |
| Candidate output | =/nix/store/4a502xwqahv4q20niz34215l1iypp2zy-nixos-system-nixos-26.11.20260718.61b7c44= |
| Active closure | =31,186,123,360= bytes |
| Candidate closure | =31,186,123,600= bytes |
| Closure delta | =+240= bytes |

=nix store diff-closures= reports no package-version change between generation
=859= and the candidate.  The build produced the candidate paths but created no
profile generation and did not activate the output.

** Candidate Output Inspection - Passed
Direct output inspection proves:

- =nix-gc.service= still executes exactly
  =nix-collect-garbage --delete-older-than 30d=;
- =nix-gc.timer= still contains =OnCalendar=Sun *-*-* 04:15:00=,
  =Persistent=false=, and =RandomizedDelaySec=45min=;
- the timer now contains =[Install] WantedBy=timers.target=;
- the candidate contains
  =etc/systemd/system/timers.target.wants/nix-gc.timer -> ../nix-gc.timer=;
- that link resolves to the candidate timer unit;
- the candidate has no =timers.target.wants/nix-optimise.timer= link;
- =systemd-analyze verify= reports only the pre-existing NVIDIA
  =/var/run/nvidia-persistenced= PID-file warning;
- =systemd-analyze calendar= reports the next nominal event as Sunday,
  July 26, 2026 at 04:15 EDT.  The configured policy adds up to 45 minutes of
  randomized delay; normal systemd timer accuracy also applies.

That calendar output describes the candidate policy, not a live trigger.

** Post-Build Live Non-Activation Proof - Passed
After the build:

- =/run/current-system= and the system profile still resolve to accepted
  generation =859= at =3947ayk...=;
- =/run/booted-system= remains generation =856= at =8q4h4b...=;
- the candidate output is not live;
- all 12 system generations =848-859= remain, including protected
  generations =856-859= at their original store paths;
- the live timer remains =linked= and =inactive (dead)=, with no next or last
  trigger;
- the live timer remains absent from =timers.target.wants=, the target
  dependency graph, and =list-timers --all=;
- the candidate alone contains the new target wants link;
- the service journal still contains exactly one start, the accepted manual
  run from =13:32:09-13:32:39 EDT=; no second GC ran;
- all 12 UKIs retain manifest fingerprint =911a636...= and all 50 legacy
  entries retain fingerprint =f2e2828...=;
- no failed system or user units appeared;
- the live checkout is clean at source head =e51f91a=.

Opening the Nix store for evaluation and building recreated the expected
=/nix/var/nix/db/reserved= emergency file at =8,388,608= bytes.  The first GC
record already predicted that later store initialization could recreate it;
this is not collection, profile mutation, or activation.

** Source/Build Boundary Closed
No =nrs=, =nixos-rebuild switch=, =test=, =boot=, direct
=systemctl enable/start=, daemon reload, second GC, optimization, cleanup, or
reboot occurred during the source/build slice.  Activation remained separately
gated until the user accepted checkpoint =9d45149= and then explicitly
authorized the next step.

** Separately Authorized Activation - Completed
At =2026-07-19 14:53:16 EDT= the final pre-activation snapshot proved:

- the clean live checkout was at accepted head =4aa203e=;
- pure evaluation of that exact head still returned the already built
  =4a502xw...= output;
- generation =859= remained current, generation =856= remained booted, and all
  generations =848-859= were present;
- the live generation =859= timer was still inactive and unarmed;
- the GC service journal still contained exactly one start, the accepted manual
  run, and the timer journal contained no entries;
- there were no failed system or user units.

The user then ran the pure =nrs= alias.  It completed without an error and
reported:

#+begin_example
the following new units were started: NetworkManager-dispatcher.service, nix-gc.timer
Done. The new configuration is /nix/store/4a502xwqahv4q20niz34215l1iypp2zy-nixos-system-nixos-26.11.20260718.61b7c44
#+end_example

That is byte-for-byte the pure-evaluated and no-link-built candidate.  It
created system generation =860=.  The alias's normal build phase realized no
different output for activation.

The activation output's =Collecting garbage...= line belongs to Lanzaboote's
normal ESP reconciliation phase, not to =nix-gc.service= or Nix-store GC.  The
exact message comes from pinned =lzbt-systemd 1.1.0=.  Its collector walks only
Lanzaboote-managed boot paths under =/boot/EFI/nixos=, =nixos-*= files under
=/boot/EFI/Linux=, and an optional PCR-lock directory that is not configured
here.  It uses ordinary filesystem removal operations, not a Nix GC command.

This invocation apparently found no stale managed artifact to remove.  All 12
preexisting UKIs =848-859= reproduce their accepted manifest fingerprint, all
eight kernel/initrd artifacts remain, and all 50 legacy loader entries remain
unchanged.  Lanzaboote added only generation =860='s =196,784=-byte UKI.

** Runtime Verification - Passed
Read-only verification between =14:56= and =14:58 EDT= proves:

| Item | Live value |
|------+------------|
| System profile/current system | generation =860=, exact output =4a502xw...= |
| Booted system | generation =856=, =8q4h4b...= |
| Immediate accepted rollback | generation =859=, =3947ayk...= |
| Other protected rollbacks | generations =857-858= retain their original outputs |
| System generations | =13=, generations =848-860= |
| System state | =running= |
| Timer unit-file state | =enabled= |
| Timer runtime state | =active (waiting)= |
| Timer active since | =2026-07-19 14:54:19.986363 EDT= |
| Timer invocation ID | =8a903df8455d47889ab9728c1b3b23ba= |
| Effective next trigger | =2026-07-26 04:56:30 EDT= |
| Last trigger | unset |
| Accuracy/random delay | =1min= accuracy, =45min= randomized delay, fixed delay disabled |
| Service starts after activation | zero |
| GC service starts in the documented July 19 window | one, the accepted =13:32= manual run |
| Failed units | zero system, zero user |

The live timer bytes are exactly the built bytes:

- =OnCalendar=Sun *-*-* 04:15:00=;
- =Persistent=false=;
- =RandomizedDelaySec=45min=;
- =[Install] WantedBy=timers.target=.

The live link
=/etc/systemd/system/timers.target.wants/nix-gc.timer -> ../nix-gc.timer=
resolves to the built =s00a15w...-unit-nix-gc.timer=.  The timer is present in
=timers.target='s dependency graph and =list-timers --all=.  Its actual
=04:56:30= trigger is 41 minutes 30 seconds after the nominal =04:15= event,
within the configured randomized delay; normal timer accuracy also applies.

No immediate Nix GC occurred:

- =nix-gc.service= remains inactive;
- the pre-activation journal cursor has zero later service lines;
- the documented July 19 journal window still contains exactly one collector
  start;
- =LastTriggerUSec= remains empty;
- the only new timer journal line is =Started nix-gc.timer=.

The =Persistent=false= no-catch-up contract therefore behaved as intended.
The timer is now armed and is currently scheduled for its displayed future
trigger.  That trigger can be recalculated after timer, daemon, or reboot state
changes; disarming it requires a separate change.

Boot and user-state preservation also passed:

- Secure Boot remains enabled;
- generation =860= is the configured default and generation =856= remains the
  selected/booted entry;
- UKIs increased only from =12= to =13= with the new generation =860= file;
- =/boot= allocated use increased by =200,704= bytes and remains =19%= used;
- all 50 legacy entries retain fingerprint =f2e2828...=;
- no boot-retention cleanup occurred;
- the Home Manager root remains =q303rn...=;
- the user =nixos-activation.service= is successful with
  =ExecMainStatus=0= and =NRestarts=0=;
- =nix-optimise.timer= remains absent, with no target link, list-timer entry,
  or service journal activity;
- the checkout remains clean at activation head =4aa203e=.

No second GC, optimization, reboot, manual profile/root deletion, or
boot-retention slice occurred.

** Current Gates
| Gate | Status |
|------+--------|
| Source and pure evaluation | =PASSED= |
| Pure no-link build | =PASSED= |
| Candidate output inspection | =PASSED= |
| Build-candidate user acceptance | =PASSED= at evidence checkpoint =9d45149= |
| Separately authorized activation | =PASSED= |
| Runtime timer verification | =PASSED= |
| User acceptance of active generation/timer | =PASSED= at evidence checkpoint =f10e1ef= |

The user explicitly accepted evidence checkpoint =f10e1ef=.  This closes the
automatic timer-arming slice.  The timer remains armed and currently scheduled
for the displayed July 26 trigger.  Store optimization and boot-entry retention
remain independent, not-yet-authorized maintenance slices.

A later measured store-optimization preflight is recorded separately in
[[file:2026-07-19-msi-nix-store-optimization.org][the MSI Nix store
optimization handoff]].  It does not reopen or change this accepted GC
checkpoint.
