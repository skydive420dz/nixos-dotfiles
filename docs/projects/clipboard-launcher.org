* Clipboard and Launcher
:PROPERTIES:
:CUSTOM_ID: clipboard-and-launcher
:END:
** Current
:PROPERTIES:
:CUSTOM_ID: current
:END:
- Launcher is native Quickshell again. =scripts/launcher-toggle= signals the =qs-launcher= overlay through =$XDG_RUNTIME_DIR=.
- The launcher searches desktop apps from =scripts/launcher-apps=.
- Extra terminal-first tools that do not ship desktop entries live in =config/quickshell/modules/launcher/apps.json=.
- =scripts/launcher-fuzzel= preserves the old fuzzel app launcher as an explicit fallback.
- Clipboard is still script-backed for now: =scripts/clipboard-toggle= uses =cliphist list | fuzzel --dmenu= and decodes the selected item back to =wl-copy=.

** Commands
:PROPERTIES:
:CUSTOM_ID: commands
:END:
- =systemctl --user restart quickshell=
- =~/.config/scripts/launcher-toggle=
- =~/.config/scripts/launcher-fuzzel=
- =~/.config/scripts/clipboard-toggle=

** Important Files
:PROPERTIES:
:CUSTOM_ID: important-files
:END:
- [[../../config/quickshell/modules/launcher/LauncherButton.qml][Launcher button]]
- [[../../config/quickshell/root/Launcher.qml][Launcher root surface]]
- [[../../config/quickshell/modules/launcher/LauncherView.qml][Launcher view]]
- [[../../config/quickshell/modules/launcher/apps.json][Launcher custom apps]]
- [[../../scripts/launcher-toggle][Launcher toggle]]
- [[../../scripts/launcher-fuzzel][Launcher fuzzel fallback]]
- [[../../scripts/clipboard-toggle][Clipboard toggle]]
- Future Quickshell clipboard surface should live under =config/quickshell/root/Clipboard.qml=.
- Future clipboard implementation modules should live under =config/quickshell/modules/clipboard/=.

** Decisions
:PROPERTIES:
:CUSTOM_ID: decisions
:END:
- Keep launcher and clipboard as separate tools until focus restoration, keyboard navigation, and close behavior are boring and reliable.
- Keep explicit fuzzel fallback scripts until the native Quickshell tools are proven boring and reliable.
- Use Quickshell screen binding for layer windows.
- Keep native launcher results apps-only by default. Window search is noise for the current workflow and can return later as an explicit mode if it earns its place.
- Follow the Quickshell ownership rules: =root/= owns surfaces, =modules/<name>/= owns behavior, =common/= owns reusable primitives only.
- Avoid full-screen invisible masks unless the overlay is intentionally click-out-to-close and the mask is verified not to steal unrelated input.

** Next
:PROPERTIES:
:CUSTOM_ID: next
:END:
- [X] Search git history/reference branches for the old launcher and clipboard implementation.
- [X] Inventory reusable launcher behavior: activation flow, app model, fuzzy scoring, keyboard navigation, focus/close handling, and theme usage.
- [X] Rebuild the launcher first as a native Quickshell surface while preserving the script fallback.
- [ ] Open native launcher with the normal launcher bind.
- [ ] Type a query and confirm matching apps appear.
- [ ] Confirm =BlueTUI=, =WireMix=, and =Neovim= appear from the custom launcher app list.
- [ ] Navigate rows with arrow keys.
- [ ] Navigate rows with =Ctrl-j= and =Ctrl-k=.
- [ ] Press =Enter= on an app result and confirm it launches.
- [ ] Press =Esc= and confirm the launcher closes.
- [ ] Click outside the launcher and confirm it closes without stealing later input.
- [ ] Run =~/.config/scripts/launcher-fuzzel= and confirm the fallback launcher still works.
- Rebuild clipboard second, with text history first and image preview only after text selection is reliable.
- Revisit unified source search only after the separate tools feel boring and reliable.
